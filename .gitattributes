# 🔴🔴 2026-09-06:影子實驗的公開檔案**不准做換行轉換**。
#
# 為什麼:這些檔案的 sha256 被寫進錨定鏈,而公開頁宣稱「帳本不可竄改」。
# `core.autocrlf=true` 會讓「工作區的位元組」與「git 存的 / GitHub Pages 送的
# 位元組」不同 —— 於是鏈上記的雜湊跟任何人下載到的檔案永遠對不上,
# 我們自己的驗證器會判「✗ 不符」,而檔案一個位元組都沒被人動過。
#
# ⭐ 假警報也是傷害;一個會誣指自己的驗證器比沒有驗證器更糟,
#   它會訓練所有人忽略紅燈。
#
# `-text` = 位元組原樣存、原樣取出,三邊(工作區 / blob / 網站)完全一致。
data/shadow/** -text
