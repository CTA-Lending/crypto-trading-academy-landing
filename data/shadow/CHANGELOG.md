# CHANGELOG — 9 腦影子競技場 Run #1

> 本檔依 **PROTOCOL §9「中途修改政策」** 建立:
> 修補「會造成錯誤估值的 bug」是**允許**的,但必須 **① 記錄於 CHANGELOG ② 標註生效日
> ③ 修改前後的樣本分開統計,不得混為一談**。
>
> 🔴 本檔在 2026-08-03 才建立 —— **這件事本身就是一項違規**。
> 2026-07-25 的修補與 2026-07-28 的缺失日,依 §9 與 §6 當時就該記錄,而沒有。
> 我們選擇把它補齊並公開,而不是讓它留在 git 歷史裡等人發現。

**本檔一經建立即為 append-only。既有條目不修改、不刪除;更正以新條目追加。**

---

## CHANGELOG-000 · 追溯登錄:2026-07-25 的報酬序列修補(**逾期揭露**)

| | |
|---|---|
| 實際生效日 | **2026-07-25**(程式 mtime 20:51Z),影響資料自 2026-07-26 發布起 |
| 揭露日 | 2026-08-03(**逾期 9 天**) |
| §9 分類 | 允許類別:「修補會造成錯誤估值的 bug」 |

**發生什麼**:`history[].returns` 欄位在前 4 個決策日記錄的數值,與淨值序列推導出來的
日報酬**不一致**。2026-07-25 修補寫入端(`forward_multi.py` 在 SETTLE 前先快照 `prev_equity`)
之後,兩者從 2026-07-26 起完全吻合。

**影響範圍**(分母 = 12 決策日 × 9 競爭者 = 108 格):
- 不一致 **22 格 = 20.4%**,全部集中在 2026-07-23 / 24 / 25
- 該三日的 27 格中有 22 格受影響(**81.5%**)
- 最大偏離:2026-07-25 等權多頭 帳載 `+0.09%` / 正確 `+29.68%`(差 29.59pt)
- 2026-07-26 之後 **0 格**不一致

**已對外發布過錯值**:錯誤數值曾出現在公開的 `dashboard_data.json` 約 72 小時
(2026-07-23 08:36Z → 2026-07-26 08:36Z),並由 `lab.html` 的「每日損益紀錄」表渲染。
2026-07-26 起改為正確值,**當時未作任何說明**。

**為什麼這個修補本身是合規的**:PROTOCOL §6 早已把報酬序列鎖死為
`equity_t / equity_{t-1} − 1`。該次修改是**把程式改回符合預先登記的定義**,
不是更改指標。違規的只有「沒有揭露」。

**淨值序列未被追溯改寫**:逐 commit 比對 `equity_curve` 的重疊前綴,12 個 commit 全部一致。

---

## CHANGELOG-001 · 🔴 資金費被乘以 3(**重大**,影響主指標)

| | |
|---|---|
| 生效日 | **2026-08-04**(下一個決策日) |
| 發現日 | 2026-08-03 |
| §9 分類 | 允許類別:「修補會造成錯誤估值的 bug」 |
| 影響期間 | **2026-07-22 ~ 2026-08-03(第 1~12 決策日)全部受影響** |

**根因**:`shadow/multi.py::funding_cost()` 內為 `total += u * p * r * 3`。
那個 `× 3`(一天 3 期 × 8 小時)在 `r` 是**單一即期費率**時是正確的;
但 v3 之後傳入的 `r` 來自 `universe.accumulate_funding()`,
**它回傳的已經是「窗口內所有結算的加總」** → 重複換算,**超收/超付 3 倍**。

`fetch_funding_since()` 的 API 失敗退路回傳 `float(r) * 3`(該處 `r` 確為即期費率,
所以那個 ×3 是對的),經過 `funding_cost` 後變成 **×9**。

**量級**(公開端點 `fapi/v1/fundingRate` 實查,2026-07-24 UTC 全日):
```
DEXEUSDT   24 筆結算 / 間隔 1h / 當日加總  -7.618%
```
單一標的單日資金費即達 -7.6%,乘 3 後 -22.9%。**這是百分點級,不是捨入誤差。**

**對主指標的影響**:預先登記的主指標是「相對 `bench_eqw` 的 Information Ratio」,
而 `bench_eqw` 100% 做多的正是資金費深度為負的標的(空方付多方)→ 超收在多頭身上
變成**超付的紅包**。因此**基準本身被高估**,所有相對它的 IR 被系統性壓低。

**修法**:移除 `funding_cost` 內的 `× 3`。日/期換算的責任歸於取數層。

> ⚠️ **修正後的重述數列尚未由本團隊獨立重跑驗證,因此本文件不列出任何重述後的數字。**
> 重跑完成後將以 **CHANGELOG-006** 追加,並依 §9 分段呈現。
> 在此之前,**第 1~12 決策日的損益數字不得用於任何對外績效陳述**。

---

## CHANGELOG-002 · 🔴 資金費抓取分頁不足導致時間軸錯位(**重大**)

| | |
|---|---|
| 生效日 | **2026-08-04** |
| §9 分類 | 允許類別:「修補會造成錯誤估值的 bug」 |
| 影響期間 | 2026-07-22 ~ 2026-08-03,**僅影響結算間隔 < 8 小時的標的** |

**根因**:`shadow/universe.py::fetch_funding_since()` 使用 `limit=20` 單次抓取。
但幣安的 `fundingIntervalHours` 並非每檔都是 8 小時 —— 公開端點 `fapi/v1/fundingInfo` 實查:

| 標的 | 間隔 | 每日結算筆數 |
|---|---|---|
| DEXEUSDT / COTIUSDT / BANKUSDT | **1 小時** | **24** |
| RIFUSDT / ESPORTSUSDT / AKEUSDT / EULUSDT | 4 小時 | 6 |
| WLDUSDT / ETHUSDT 等 | 8 小時 | 3 |

一天 24 筆卻只抓 20 筆 → cursor 每天落後 4 小時,而且**逐日累積**。
後果不是「少算」,而是**兩天前的極端費率被套用在今天已經改變的部位規模上** ——
時間軸本身錯位,**無法用固定係數校正**。

**修法**:改為分頁抓取(`limit=1000`,迴圈至追上或無新資料,50 頁上限防呆),
`accumulate_funding()` 的 exactly-once cursor 語意不變。

---

## CHANGELOG-003 · 公開的兩個 JSON 對同一格給出互斥數值

| | |
|---|---|
| 生效日 | **2026-08-03**(本次發布即生效) |
| §9 分類 | 允許類別:修補錯誤估值 |

**發生什麼**:同一次推送(commit `26615ed`, 2026-08-03 08:36Z)同時發布:
- `dashboard_data.json` → 2026-07-25 規則腦 **-28.18%**、等權多頭 **+29.68%**(正確)
- `diaries.json` → 同日同腦 **-0.395%** / **+0.09%**(錯誤)

**根因**:CHANGELOG-000 的修補只改了寫入端,`journal[].outcome` 的**歷史值未回填**,
而 `diaries.py` 讀的正是它。今日發布的 10 日滾動視窗 × 9 競爭者 = 90 格中,
**16 格(17.8%)仍為錯值**。

`diaries.json` 未被任何 HTML 連結,因此團隊內部無從察覺其損壞。

**修法**:`diaries.py` 改為與儀表板共用同一個權威來源
(`daily_returns.daily_return_table`,即 PROTOCOL §6 鎖定的 `equity_t/equity_{t-1}−1`)。
**刻意不回填 `journal`** —— 那是已被外部錨定的原始紀錄,不應追溯改寫。

**驗證**:修補後逐格比對兩檔,分母 90 格 → **不一致 0 格**。

---

## CHANGELOG-004 · `temperature = 0.0` 從未被實際送出(**紀錄更正**)

| | |
|---|---|
| 生效日 | **2026-08-03**(僅更正紀錄,**不改變受試者行為**) |
| §9 分類 | 紀錄更正 |

**發生什麼**:`forward_multi.py` 的 `LLM_PARAMS` 記錄 `"temperature": 0.0`,
且該 dict 每日寫入帳本並納入外部錨定。但實際發出請求的
`sliceb/llm_router.py::call_llm()` **函式簽名中沒有 temperature 參數**,
request body 亦未攜帶 → 第 1~12 決策日全部運行於各供應商的預設行為。
**⚠️ 確切數值未由本系統記錄,因此不得宣稱「實際為 1.0」** —— 可確認的只有「未送出」。

**後果**:一個宣稱不可竄改、每日錨定的參數,記錄的是一件從未發生的事。
**本 Run 的 LLM 決策不可重現。**

**修法選擇說明**:**刻意不改為「真的送出 temperature=0」**。
那會在實驗進行中改變受試者行為,違反 §9「禁止修改受試者」。
正確處理是把紀錄改為真實描述,行為維持原樣。
決定性(temperature=0)留待 **Run #2** 預先登記。

---

## CHANGELOG-005 · 每日資金費未留痕(可稽核性缺陷)

| | |
|---|---|
| 生效日 | **2026-08-04** |

`funding_cost()` 的計算結果過去直接從現金扣除,**未寫入帳本任何欄位**。
因此 CHANGELOG-001 的錯誤**無法從帳本自身重建**,只能靠重新抓取行情重跑。
一個影響損益的數字沒有留痕,即為不可稽核。

**修法**:每日各競爭者的資金費金額寫入 `history[].funding`。

---

## 缺失日登錄(PROTOCOL §6 要求,**逾期揭露**)

| 日期 | 狀態 | 說明 |
|---|---|---|
| **2026-07-28** | 未執行 | stdout log 顯示 `^C` → **啟動後遭手動中斷**。無告警、無紀錄。 |

依 §6:「該日整體不執行 → 全體同時剔除(不補值、不前推);記於 CHANGELOG」——
剔除已正確執行(9 個競爭者全體無該日資料),**但當時未記於 CHANGELOG**,本次補登。

🔴 **連帶影響**:`daily_returns_from_equity` 以相鄰 `history` 相除,不具日期間距概念,
因此公開表上 **2026-07-29 該列實際涵蓋 48 小時**(2026-07-27 00:36Z → 2026-07-29 00:36Z),
與其餘 24 小時的列並排而未標示。錨定鏈 `seq6 → seq7` 相隔 48h04m 可證(其餘各格 24h±35s)。
把 2 日報酬混入 1 日序列會**低估波動、高估 Sharpe 與 IR**。此項待 CHANGELOG-006 一併處理。

---

## 已知待處理(尚未修補,列此以免遺漏)

1. **兩套年化因子並存**:`stats.py` 用 √252(預先登記),
   `emit_dashboard.py` / `forward_multi.py::metrics()` 用 √365(對外發布的)。
   公開頁上的 Sharpe 與最終統計的 Sharpe 不是同一個數字。
2. **擁擠度門檻無依據**:`multi.py` 的 `avg >= 60` 為硬編碼,程式與 PROTOCOL 皆無推導。
   同期實測日報酬相關係數(n=12):GPT–Gemini **+0.810**、GPT–Claude +0.757、
   Gemini–Claude +0.594(3 對平均 **+0.72**)。**持倉重疊度會系統性低估真實擁擠。**
   目前公開頁對 19.0% 標示「✅ 分散」,此一保證缺乏依據。
3. ~~規則腦與基準不經過曝險檢查~~ → **已改為揭露,見 CHANGELOG-006。**
4. **主指標 IR 依 §6 於第 90 決策日結算後才計算**,目前公開頁顯示者皆為 §3 允許的
   描述性次要指標。

---

## CHANGELOG-006 · 曝險檢查的適用範圍未揭露(**陳述更正**)

| | |
|---|---|
| 生效日 | **2026-08-04**(僅更正陳述,**不改變任何受試者行為**) |
| §9 分類 | 陳述更正 |

**發生什麼**:總曝險上限(100%)的檢查位於 `multi._parse_alloc()` 內,
而該函式**只被 `llm_allocation()` 呼叫**。因此:

| 受試者 | 是否經過曝險檢查 |
|---|---|
| gpt / gemini / claude / gpt_pure / guardian(5 個 LLM 腦) | ✅ 是 |
| **rule(規則腦)** | ❌ **否** |
| bench_btc / bench_eqw / bench_cash(3 個市場基準) | ❌ 否 |

`rule_allocation()` 與 `benchmark_allocation()` 直接回傳權重,不經過解析與檢查。
公開資料檔 `dashboard_data.json` 的 `violations` 中 rule = 0,
**代表「未受檢查」,不代表「未逾越」**。

實測:規則腦於 **2026-07-25** 的 mark-to-market 實際總曝險達 **174.6%**,
當日無任何違規紀錄或告警。

**修法選擇說明**:依 §9「**禁止**修改風險上限」,不得於 Run #1 進行中對 rule 新增約束
—— 那會改變受試者行為。因此只做兩件事:
1. 資料檔新增 `violation_checked` 欄位,明確標示每個受試者是否受檢查
2. 公開頁「紀律紀錄」區塊加註適用範圍與 174.6% 的實測值

即:**改的是裁判的記分板,不是受試者的行為。**

**誠實補充**:公開頁的違規表格原本就只列出 5 個 LLM 腦,rule 與基準並未出現在該表,
因此該表本身未曾對 rule 作出「0 違規」的陳述。真正缺乏揭露的是
「這個量測只適用於一部分受試者」這件事,以及公開資料檔中的 `violations` 欄位。

---

## CHANGELOG-007 · 🔴 資金費取數層的兩個靜默失效路徑(**未曾觸發,預防性修補**)

| | |
|---|---|
| 實際生效日 | **2026-08-21** |
| 揭露日 | 2026-08-21(同日) |
| §9 分類 | 允許類別:「修補會造成錯誤估值的 bug」 |
| 是否影響既有樣本 | **否** —— 兩條路徑在 Run #1 全部 30 個決策日中**一次都沒有觸發**(`data/multi_daily.log` 中「資金費近似」出現 0 次),故不切分樣本、不作廢任何日期 |

**起因**:CEO 於 2026-08-21 要求「幣圈爆漲,查影子實驗室結算報價是否正常」。
報價本身查無異常(逐檔對幣安即時價,18 檔最大偏差 2.9%,無凍結價;
資金費符號與量級以原始結算歷史獨立重算,−7.08 vs 帳上 −7.40,差異由
「系統用重配置前的舊倉、複算用重配置後的新倉」完全解釋)。
異常出在**翻閱取數層時發現的兩條從未走過的路**。

**洞一 · 靜默丟棄**:`universe.fetch_funding_since()` 的 except 分支中,
歷史與當期兩條路都失敗時是 `continue` —— 該幣直接從回傳 dict 消失。
呼叫端 `rates.get(b)` 取得 `None`,`multi.funding_cost()` 跳過它,
**該幣當日資金費為 0**。而警示清單以 `fr.get(sym, {}).get("approx")` 計算,
對不存在的 key 回 `None`,因此**它連警示清單都進不去**。
實測後果:18 檔中漏 1 檔 DOGE(名目 40,500)= 少收 36.45 USDT,無任何紀錄。

**洞二 · 退路重複計費**:退路回傳 `即期費率 × 3` 供呼叫端扣款,同時
(正確地)不推進 cursor 以便回補。但隔日恢復時,`accumulate_funding` 從舊
cursor 起算,**把同一批結算再收一次**。實測:每日 3 期 × 0.01%,壞一天再恢復
→ 兩日合計收 0.0009,真實應為 0.0006,**多收 50%**。

**洞三 · 守衛單向**:`invariants.FUNDING_MAGNITUDE` 只擋「金額 > 淨值 15%」。
金額 0 永遠通得過。一道只覆蓋單一方向的判準,擋不住相反方向的失效。

**修補**:
1. 取數層改為**契約保證每檔都回**;失敗時回一筆標記過的零
   (`approx=True`、`failed=True`、附 `spot_hint` 診斷值、cursor 不前進)。
2. 退路**一律不計費**(`total=0`)。cursor 未前進,金額由下一次成功的窗口
   完整補齊 —— 寧可「日期錯一天」也不要「金額錯一倍」:前者可稽核,後者會複利。
3. 新增純函式 `multi.funding_coverage()`,以**獨立於金額的來源**量測涵蓋率,
   分開回報 approx / failed / missing 三種不同的失效,寫入每日紀錄與 run_meta。
4. 不變式補三條雙向守衛:`FUNDING_COVERAGE`(漏檔)、`FUNDING_UNREACHABLE`
   (問不到)、`FUNDING_INCOMPLETE`(不完整比例 > 20% 為 critical,否則 warn)。

**驗證**:新增 `tests/test_funding_coverage.py` 17 條。
六道守衛逐一以突變測試反向對照 —— **每一道拿掉,測試都會紅**
(3 / 2 / 2 / 1 / 1 條失敗)。全案測試 3,403 → 3,420 條,全過。

**為什麼記在這裡**:這兩條路徑從未觸發,依 §9 嚴格說可不記錄。但
CHANGELOG-001 與 -002 都是資金費的錯,且都在**已經影響 12 個決策日之後**
才被發現。「問不到」與「沒有資金費」在損益上長得一模一樣,是本專案
第八次踩到的同一個盲點 —— 這種盲點值得在它咬人之前就留下紀錄。

---

## CHANGELOG-008 · 🔴 基建故障會被記錄成流動性事件並砍 20%(**未曾觸發,預防性修補**)

| | |
|---|---|
| 實際生效日 | **2026-08-21** |
| 揭露日 | 2026-08-21(同日) |
| §9 分類 | 允許類別:「修補會造成錯誤估值的 bug」 |
| 是否影響既有樣本 | **否** —— Run #1 全部 30 個決策日中 `[STALE]` 出現 0 次、強制平倉 0 次 |

**發現路徑**:與 CHANGELOG-007 同一次稽核。查完資金費後回頭問一個對稱的問題:
報價取數層(`fetch_execution_prices`)有沒有同一個毛病?有,但方向相反 —— 而
方向相反的那一種更貴。

**問題**:`fetch_execution_prices()` 整包 `fetch_tickers` 失敗、逐檔重試又全失敗時,
回傳空 dict。下游 `run_day()` 的判斷是 `missing = [x for x in units if x not in px]`
—— 於是**每個腦的每一檔持倉同時變成「無報價」**。接著:

- 連續 `STALE_MAX_DAYS = 3` 日 → **全體強制平倉**,套用 `STALE_EXIT_HAIRCUT = 20%` 折價
- 若連最後已知價都沒有(`still` 非空)→ **當天立刻強平**,沒有 3 日緩衝

也就是說:**交易所 API 連續掛三天,帳本就永久少 20%,並且被記錄成
「無報價資產流動性已死」的流動性事件。** 一檔幣沒報價是市場的事;
十六檔幣同時沒報價是我們這邊的事 —— 後者不該由受試者付代價。

這是本專案第九次踩到「查不到 / 問不到 / 沒有」三者被當成同一件事的盲點。

**修補**:
1. 新增純函式 `multi.quote_health()`:報價涵蓋率 < 50% 判定為**基建故障**而非
   流動性事件(此量級的同時失效在幣安永續上不可能是市場成因)。
2. 新增純函式 `multi.stale_action()`:把「缺報價該怎麼處理」從 `run_day()` 的
   迴圈裡抽出來 —— 原本那段**沒有任何測試碰得到**(需網路 + LLM 才跑得起來)。
   故障期間:降級不消音(照樣回報缺哪幾檔)、倒數不前進、不強平。
3. 不變式新增 `QUOTE_OUTAGE`:第一天 warn、**連續第二天起 critical**。
   「維持原倉」如果沒有上限,就會變成無限期裝沒事。
   三要點取自訊號機停機宣告事故:**fail-safe(壞掉照舊吵)、有上限、降級不消音**。
4. 每日紀錄新增 `quote_outage` / `quote_coverage` / `quote_outage_days` 留痕。

**驗證**:
- 重構等價性:窮舉 **64 格**(持倉 × 有報價的 × 有最後已知價的 × 前次天數),
  非故障情況下新舊行為**逐格相同** —— 重構不准偷改行為。
- 突變測試:**11 道守衛逐一拿掉,每一道都會紅**(含 CHANGELOG-007 的 6 道)。
- 30 個真實歷史決策日跑新守衛:**0 誤報**(誤報也是傷害)。
- 全案 3,403 → **3,431 條測試全過**。

---

## CHANGELOG-009 · 🔴 換倉成本扣了錢卻沒有留痕(**可稽核性缺陷,同一課學第二次**)

| | |
|---|---|
| 實際生效日 | **2026-08-25** |
| 揭露日 | 2026-08-25(同日) |
| §9 分類 | 可稽核性修補(不改任何既有數字) |
| 是否影響既有樣本 | **否** —— 成本本來就有扣進淨值,這次只是把它**記下來**。既有 34 日的損益不變,但那 34 日**無法從帳本拆出成本** |

**起因**:CEO 2026-08-25 問「等權多頭與規則腦為什麼虧比較多」。
我打開帳本想拆帳,**拆不了** —— `forward_multi.py` 裡
`st["equity"][b] = eq_pre - cost` 把換倉成本扣掉之後,`cost` 就消失了,
日紀錄裡沒有這個欄位,只能靠重抓行情推論。

**這跟 CHANGELOG-005 是一模一樣的錯。** 那次(2026-08-03)是每日資金費沒留痕,
導致「資金費被乘 3 倍」無法從帳本重建。當時寫下的結論是:

> 「一個影響損益的數字沒有留痕,就是不可稽核。」

然後換倉成本犯了同一個錯,而且活了 **34 個決策日**沒被發現 ——
因為在有人問「為什麼虧」之前,沒有人需要拆帳。

**修補**:
1. 日紀錄新增 `換倉成本` 與 `換手金額` 兩欄。
2. 換手金額在**呼叫端**計算,不動 `rebalance_to_target()` 的兩值回傳契約
   (它有多個呼叫端與測試在做兩值解包)。
3. 🔴 換手的快照取在**重配那一行的前面**,不是結算之前 ——
   結算階段的強平/清倉也會改動部位,但那不是換倉,
   把它算進來會高估換倉成本。
4. 不變式新增兩條:`COST_WITHOUT_TURNOVER`(沒換手卻扣了成本)、
   `COST_MAGNITUDE`(成本 ÷ 換手 > 1%,而費率+價差的實際量級是 0.07~0.17%)。

**驗證**:`tests/test_turnover_cost_ledger.py` 7 條;兩道新守衛逐一以突變測試
反向對照(拿掉都會紅);34 個真實歷史決策日 **0 誤報**;全案 3,478 → **3,485** 全過。

**同時回答了 CEO 的問題(用市場資料,不是用帳本)**:
等權多頭與規則腦虧得多,主因是**它們持有的幣波動度極高** ——
持倉年化波動中位數:BTC持有 43% / Claude 112% / GPT 120% / Gemini 150% /
**等權多頭 331% / 規則腦 380%**。波動度與績效幾乎完全反向排序。
根因在候選表的排序 `-abs(ret30d_pct)`:**按「絕對」漲跌幅排序,
本質上就是在挑波動最大的幣**,而等權多頭機械地取前 10 檔全部做多。
⚠️ 另有一個目前尚未觸發的缺陷:`abs()` 讓**下跌 60% 的幣**也排在最前面,
在混合行情下等權多頭會去「做多」那些暴跌的幣。今日全數為正,故未咬到。


---

## CHANGELOG-010 · 🔴 公開頁停止更新 33 小時,而**沒有任何人被通知**(發佈鏈路故障)

| | |
|---|---|
| 發生期間 | **2026-09-03 00:36Z ~ 2026-09-04 10:08Z**(約 33.5 小時) |
| 揭露日 | 2026-09-05 |
| §9 分類 | 不屬修改;為**可用性與揭露缺陷**登錄 |
| 對數據的影響 | **無**。實驗照常每日執行,第 43、44 決策日的帳本、錨點都在當時正常產生 |
| 對揭露的影響 | 🔴 **有**。公開頁在這 33.5 小時內顯示的是第 42 決策日的資料,而頁面上**沒有任何地方說它是舊的** |

**發生什麼**:公開頁停在 2026-09-02 的資料。第 43、44 日的 commit
(`03e2d9b`、`47aee49`)在本機正常產生,但 **`git push` 被拒絕**。

**根因不是這支程式壞了,是它的前提被別人改掉了。**
`shadow/anchor.py` 一直是 `commit` → `push`,中間**沒有 pull**。
這在「只有我一個寫入者」的前提下是對的。
而 2026-09-02 起,**山寨季雷達開始往同一個 repo 每日發佈**
(`feb2a97` 起共 9 筆),前提消失 → non-fast-forward → 推送被拒。

🔴 **最嚴重的不是推不上去,是沒有人知道。**
`anchor()` 把失敗寫成回傳值裡的 `pushed=False`,呼叫端
`forward_multi.py` 只是 `log(...)` 一行,**沒有告警、沒有不變式、沒有任何管道會出聲**。
所以它安靜地失敗了兩次,是 **CEO 自己打開網頁看到日期不對**才被發現的。

⭐ 這是「共用資源、兩個寫入者」的經典形狀,也是本專案第五次
「**拿日誌安靜當成沒事**」—— 日誌沒人看的時候,寫進日誌等於沒寫。

**修法**(2026-09-04 已上線):
1. `anchor.py` 推送失敗後 `fetch` + `rebase --autostash origin/main` 重試一次;
   rebase 失敗就照實回報,**絕不 force**。
2. 🔴 為什麼 rebase 對證據鏈是安全的:錨定鏈記的是**內容雜湊**
   (`ledger_head` / `tree_root` / `protocol_sha256`),**不記 git commit hash**。
   重寫 commit 不影響鏈的任何一個欄位。

**尚未修的**(列此以免遺漏):`pushed=False` 目前仍然只寫日誌,
**沒有升級成會吵人的告警**。下一次同類故障仍然只能靠人眼發現。

---

## CHANGELOG-011 · 第 44 決策日有**三筆**錨點,以及一支任何人都能跑的鏈驗證器

| | |
|---|---|
| 事件日 | 2026-09-04 |
| 揭露日 | 2026-09-05 |
| 對數據的影響 | **無** |

**現象**:錨定鏈在第 44 決策日有三筆:

| seq | UTC | ledger_head | tree_root |
|---|---|---|---|
| 44 | 2026-09-04T00:37:00Z | `2c906a0276d39799…` | `b1a7f751d7b6968c…` |
| 45 | 2026-09-04T10:08:09Z | `2c906a0276d39799…` | `fb874b85958e1515…` |
| 46 | 2026-09-04T10:08:52Z | `2c906a0276d39799…` | `fb874b85958e1515…` |

seq 45/46 來自 CHANGELOG-010 的**人工補推**(修好 `anchor.py` 之後手動跑了兩次)。

🔴 **三筆的 `ledger_head` 完全相同 —— 帳本一個字都沒有被重述。**
不同的只有 `tree_root`(程式碼在 00:37 與 10:08 之間改過,就是那個修補本身)。
也就是說鏈忠實記錄了「同一份帳本、程式碼變了、當天發佈了三次」。

⭐ 但**在這一則之前,鏈上沒有任何地方解釋這件事**。
一個外部讀者看到同一天三個錨點,在沒有說明的情況下,
合理的懷疑是「他們重跑到滿意為止」。**沉默本身就是問題。**

### 🔴🔴 更嚴重的:在 2026-09-05 之前,這條鏈**從來沒有被任何程式驗證過**

公開頁上印著一句宣稱:「✓ 帳本不可竄改(hash chain)」。
而 `shadow/anchor.py` 只有 `append_chain()`(寫),**沒有任何驗證函式**。
我們寫了一條鏈,宣稱它不可竄改,然後從來沒有檢查過它。
**一條沒有人驗證的雜湊鏈是裝飾品,不是證據。**

**修法**:新增 `shadow/verify_chain.py`,並**隨鏈一起發佈到公開 repo**
(`data/shadow/verify_chain.py`),任何人可以下載後自己跑:

```
python verify_chain.py anchor_chain.json
```

它檢查:seq 是否連續、每筆 `prev` 是否等於前一筆 `hash`、
每筆 `hash` 是否為其餘欄位的 SHA-256,並列出同一決策日的多筆錨點
(並區分「帳本被重述」與「只是重跑發佈」——兩者是完全不同的事)。

🔴 **它證明不了什麼,也一併印出來**:
雜湊鏈只防「改一筆」,**防不了「整條重算」**。
如果我們願意把整條鏈重寫,這支驗證器一樣會說通過。
防重寫要靠外部時間戳 —— 本專案是每日推送到公開 git repo,
commit 時間由 GitHub 記錄,**那份證據在 git 歷史裡,不在這支程式裡**。

驗證器本身有 16 條測試,包含反向對照組:改一筆內容、
改完重新蓋章、抽掉中間一筆、砍掉尾端、空鏈、缺欄位 —— 每一種都必須讓它變紅。
其中「整條重新蓋章」那一條**斷言它會說通過**,把能力邊界釘死。

---

## CHANGELOG-012 · 🔴🔴 我們自己違反了 CHANGELOG-001:含作廢期的全期報酬曾公開 45 分鐘

| | |
|---|---|
| 發生期間 | **2026-09-05 07:47 ~ 08:32(+08)**,約 45 分鐘 |
| 揭露日 | 2026-09-05(**當日**) |
| 性質 | **對外績效陳述違反預先登記的限制** |

**CHANGELOG-001 明訂**:

> 修正後的重述數列尚未由本團隊獨立重跑驗證,因此本文件不列出任何重述後的數字。
> 在此之前,**第 1~12 決策日的損益數字不得用於任何對外績效陳述**。

而 2026-09-05 07:47 推上公開頁的版本(commit `dd42db0`),
在每張卡片下方印了 `full_total_pct` ——
**自起始資金起算、含那 12 個已作廢決策日的全期報酬**,例如:

> 規則腦:起始資金起算 **−47.6%**(44 日,含已作廢 12 日)

`shadow/emit_dashboard.py` 的註解本來就寫著
「全期只留在 `full_total_pct` 供內部除錯,**不得對外引用**」。
改動的人讀了那支程式碼,**沒有讀到那行註解**。

**為什麼會做這件事**(理由本身是成立的):卡片上同時顯示權益 `$5,236`
與報酬 `−20.7%`,讀者一除就得到 −47.6%,不講出來會像在隱瞞。

**但正解不是把被禁止的數字印出來,而是把大字報酬的分母印出來**:

> 基準 **$6,603**(2026-08-03 收盤) → 現在 **$5,236**

讀者可以自己驗算 `5236 / 6603 − 1 = −20.7%`,
而我們**一個字都沒有對那段作廢期間的損益下判斷**。

**已於 2026-09-05 08:32(+08)撤下**(commit `3145004`),曝光約 45 分鐘。

**防止再犯**(`tests/test_lab_page_no_truncation.py`):
1. 公開頁的程式碼(剝掉註解後)不得出現 `full_total_pct` / `full_days`
2. 🔴 基準只能來自權益曲線,**取不到時必須是 `null`** ——
   任何會產生數字的 fallback 都不合法,因為拿 $10,000 當分母算出來的
   正好是那個被禁止的 −47.6%,而**畫面上不會有任何異狀**
3. 用真實資料驗算前端公式必須等於後端 `metrics[b].total_pct`(容差 0.05pp),
   並附反向對照:故意取錯一天必須超過容差,否則這道判準分辨不出 off-by-one

⚠️ 第 2 條的第一版寫成一張「壞寫法黑名單」(`|| m.start_capital` 等),
突變測試當場證明沒用 —— 寫成 `:(m.start_capital||0)` 就繞過去了。
**黑名單永遠列不完**,改成釘住那一行的結構。

### 📌 順帶登錄一項尚未兌現的承諾
CHANGELOG-001 寫著「重跑完成後將以 **CHANGELOG-006** 追加」。
而實際的 CHANGELOG-006 是另一件事(曝險檢查的適用範圍未揭露),
**編號被佔用,而重述數列至今仍未獨立重跑驗證、未發佈**。
本檔為 append-only,不修改 001 的原文;在此登錄:
**該承諾仍然未兌現**,重述數列發佈時將另編新號並在此註明。


---

## CHANGELOG-013 · 🔴🔴 已錨定的 PROTOCOL 寫「Pilot:90 個決策日」,而公開頁印「365」

| | |
|---|---|
| 發現日 | 2026-09-05(對抗式稽核) |
| 揭露日 | 2026-09-05 |
| 狀態 | **未修正,等 CEO 裁決**(兩個選項都會動到對外陳述) |

**事實**(逐字):`data/shadow/PROTOCOL.md` 第 30 行寫著

> - **Pilot:90 個決策日** = 工程與行為觀察期。**不足以判定真錢資格。**

同一份檔案第 3 行與第 129 行也各出現一次「90 天」。
而公開頁 `lab.html` 的標題印著「第 45 / **365** 日」,
`meta.pilot_days = 365`。

🔴 **問題不在於改長度本身**:PROTOCOL §9 禁止的是修改
「primary metric、基準、成本假設、風險上限、受試者名單」——
Pilot 長度不在那份清單裡,而且真錢 gate 仍是 252 日、統計方法仍鎖死,
所以延長**不構成「換指標說故事」**。

🔴 **問題在於紀錄**。這一頁在同一個畫面上寫著:

> ✗ 不事後換指標　　✓ 判定標準跑之前就公開錨定

並且**直接把 PROTOCOL 的 sha256 印出來當憑證**。
讀者順著那個 hash 點進 PROTOCOL.md,讀到「Pilot:90 個決策日」,
回頭看標題是 365 —— 他無從判斷是頁面誇大了實驗長度,
還是被錨定的協議已經過期。**任一種解讀都直接打掉那句宣稱的可信度。**

而 90 → 365 這件事(CEO 2026-08-27 決定)**從未進入本 CHANGELOG**,
PROTOCOL 也從未重新錨定。`shadow/pilot.py` 那次「把寫死的 90 收斂成單一常數」
只掃了 5 支程式碼,**沒有掃 PROTOCOL.md,也沒有掃公開頁的兩處靜態文字**
(公開頁那兩處已於 2026-09-04 改成從資料讀)。

### 兩個選項,都需要 CEO 裁決(兩者都會改變對外陳述)
| | 作法 | 代價 |
|---|---|---|
| **A** | 公開頁改回「90 日」,PROTOCOL 不動 | 與 CEO 08-27 的決定相反 |
| **B** | PROTOCOL 發 v1.1 修訂版(標明生效日與原因)、**重新錨定**,舊 hash 保留可查 | 需要說明「為什麼中途改協議」,而這正是這個實驗最敏感的地方 |

🔴 **在裁決之前,本專案不得宣稱實驗長度已經預先登記為 365 日。**
目前已錨定、可驗證的預先登記值是 **90**。

### 同時登錄:同一輪稽核查到、已修的三項
1. **`anchor.json` 的 `initial_state` 不是初始狀態**,是錨定當日的狀態。
   公開檔案因此寫著 `initial_state.rule.equity = 5115.90`,
   而頁面說「九位競爭者一律 $10,000」—— 而且錯的方向對我們有利
   (規則腦看起來本來就只有 5,115,那筆回撤就此消失)。
   已更名為 `state_at_anchor`,並新增 `start_capital_note` 指向真正的起始資金。
2. **錨定鏈只保護 PROTOCOL 與引擎程式碼**,不保護讀者實際看到的
   `dashboard_data.json` 與 `CHANGELOG.md` —— 而頁面把「帳本不可竄改」
   寫在那些數字**旁邊**。改掉任何一個數字,舊版的鏈照樣全部通過。
   已把這四個公開檔案的 sha256 納入每日錨點的 `published` 欄位,
   並讓公開的驗證器一併比對(實測:偷改一個報酬數字 → 判「不符」、退出碼 1)。
3. **鏈檔不見了會靜靜開一條新鏈**(seq 0、prev=0×64)。
   已改成:若 `anchor.json` 顯示跑過天數 ≠ 0,拒絕開新鏈並拋錯。

### ⚠️ 這一輪稽核的分母(必須講清楚)
七個維度共掃出 **102 條**,但驗證階段大量代理人中斷,
**實際完成三視角反駁的只有 9 條**(8 條通過、1 條被駁回)。
**其餘 93 條是「未驗證」,不是「已排除」。** 本條目只登錄已驗證的部分;
未驗證的 93 條另行處理,不得因為沒被驗證就當作不存在。


---

## CHANGELOG-014 · PROTOCOL 修訂為 v3.4:實驗長度 90 → 365(CEO 2026-09-06 裁決)

| | |
|---|---|
| CEO 決定延長 | **2026-08-27** |
| 本次裁決(A/B 二選一) | **2026-09-06,選 B:修訂 PROTOCOL 並重新錨定** |
| 修訂前 sha256 | `48973c29afccb2d499f509a3532d0a193e12d8a09d6d62c49ddecbe33017921a` |
| 修訂後 sha256 | `1b953e4d9e218ef06a14a5c457a8e377ab91c9e0d0b0448e8ce0be99e7757dad` |
| 舊版在哪裡驗證 | 錨定鏈 seq **0~47** 每一筆的 `protocol_sha256` 都是舊值;seq 48 起為新值 |

CHANGELOG-013 提出兩個選項,CEO 裁決 **B**:
把協議改成 365,而不是把頁面改回 90。

**改了什麼**:§5 的 Pilot 天數 90 → 365。

🔴 **什麼沒有改**(這才是「不事後換指標」的實質內容,逐項列於 PROTOCOL §0):
primary hypothesis、受試者名單、四個基準、§4 失格條件、
**真錢資格 gate ≥252 個決策日且須涵蓋規定的市場情境**、
§6 統計方法(含 Newey–West lag 規則與「樣本 <30 不報 p 值」)、
計價單位、執行價與 haircut、基建異常政策、全部成本假設。

⭐ 加長觀察期**不會**讓任何人比較容易宣稱勝負。它改變的只有「我們打算觀察多久」。

### 同時做的三件事
1. PROTOCOL 新增 **§0 修訂紀錄**(append-only),列出改了什麼、
   **沒改什麼**、舊指紋、以及舊版可在鏈上哪幾筆驗證。
   原始版本沒有這一節 —— 它是這次修訂建立的,而這次修訂就是第一筆。
2. §15 誠實聲明原本寫「**90 天**樣本在統計上不足以支撐結論」。
   🔴 **沒有把它改成「365 天不足」** —— 那會是一句我們沒有驗證過的新宣稱。
   改成「**目前的樣本長度**不足」,並指回 §6 已登記的判準。
3. 公開頁的揭露會**自動消失**:它比對的是「協議寫的數字」與「頁面用的數字」,
   兩者現在都是 365。不需要有人記得回來刪一段文字。

### ⚠️ 修訂過程中踩到、當場修掉的一個坑
§0 的敘述句寫著「§5 的 `Pilot:90 個決策日` → `Pilot:365 個決策日`」,
而後端解析協議用的是 `re.search`(取**第一個**命中)
→ **解析出 90**,而 §5 其實已經是 365。

症狀不是當機,是**頁面會永遠顯示「協議寫 90」的警語,而協議根本已經改好**。
⭐ 一個抓錯來源的解析器不會報錯,只會安靜地一直說錯話。

已改成只認 §5 那一行的**粗體權威定義**,並加一條用「含誘餌的假文件」
驗它的測試(三個方向:被誘餌騙 / 權威定義改了要跟著變 /
權威定義不存在時必須解析不到,而不是撿一個敘述句)。


---

## CHANGELOG-015 · ⚠️ 錨點 seq 48 的公開檔案雜湊對不上下載到的檔案(換行,非竄改)

| | |
|---|---|
| 影響 | 錨點鏈 **seq 48**(2026-09-06T00:36:56Z)的 `published` 欄位 |
| 揭露日 | 2026-09-06(**當日**) |
| 對數據的影響 | **無**。三個檔案一個位元組都沒有被人動過 |

**現象**:任何人在 2026-09-06 下載公開檔案並執行 `verify_chain.py`,
會看到三個檔案被判「✗ 不符」:

```
   ✓ CHANGELOG.md           鏈上 f44bdad95d0b2b21  現在 f44bdad95d0b2b21
   ✗ PROTOCOL.md            鏈上 1b953e4d9e218ef0  現在 603a62467f936a39
   ✗ dashboard_data.json    鏈上 a0454ccf0b874b04  現在 632415ebb0143f37
   ✗ diaries.json           鏈上 ac2da9fee2b2ab36  現在 ce9d3ccb30761d70
```

🔴🔴 **這是最糟的一種失敗:我們自己的驗證器在指控我們竄改數字。**

**根因**:雜湊取自**本機工作區的位元組**,而全世界下載到的是
**git 正規化之後的位元組**。Windows 上文字模式寫檔產生 CRLF,
`core.autocrlf=true` 讓 git 在 commit 時轉回 LF,GitHub Pages 送出 LF 版
→ 兩者永遠對不上。CHANGELOG.md 之所以相符,只是因為它本來就是 LF。

**可自行複現**:把下載到的檔案 `LF → CRLF` 之後取 sha256,會等於鏈上記的值。

⭐ **假警報也是傷害**,而一個會誣指自己的驗證器**比沒有驗證器更糟** ——
它會訓練所有人忽略紅燈。

### 修法(三層,全部已上線)
1. `anchor.py` 新增 `normalize_published_eol()`,在**取雜湊之前**把公開檔案
   統一成 LF。🔴 刻意**不是**「改成算正規化後內容的雜湊」——
   那樣雜湊描述的就不是任何真實存在的檔案了;而是**讓檔案本身就是 LF**,
   於是「工作區 = git blob = 網站送的」三者是同一串位元組。
2. `landing-page/.gitattributes` 加上 `data/shadow/** -text`,
   讓 git 不再對這些檔案做任何換行轉換。
3. `verify_chain.py` 會**分辨「只差換行」與「內容被改」**並印出診斷。
   🔴 但這**不是免死金牌**:仍然判為不符,只是多說一句它是哪一種。
   有反向對照測試守著:數字真的被換掉時,不准被說成「只差換行」。

### 🔴 seq 48 這一筆不會被修正
錨定鏈是 append-only。seq 48 記的雜湊就是當時算出來的值,錯了也留著。
**從 seq 49 起**(2026-09-07 的排程)雜湊會與下載到的檔案一致。
在那之前,驗證器對這三個檔案會顯示「✗ 不符(內容相同,只差換行)」。

### 為什麼沒有補跑一次錨定把它變綠
補跑會在同一個決策日多產生一筆錨點,只為了讓畫面好看。
**「重跑到滿意為止」正是這條鏈存在要防止的事**,即使這一次的動機是無辜的。
留著它,並在這裡說清楚,比多一筆漂亮的紀錄誠實。


---

## CHANGELOG-016 · 🔴 公開頁上五處「安靜地錯」(智囊團點名 → 逐條查證 → 全部修正)

| | |
|---|---|
| 發現日 | 2026-09-06(外部智囊團 GPT-6 / Gemini-3.1 共同審查) |
| 揭露日 | 2026-09-06(**當日**) |
| 對帳本的影響 | **無**。五項都是「公開呈現」的缺陷,帳本數字自始正確 |

智囊團點出五條「頁面正在說謊」的線索。**它們是線索不是結論**,
所以逐條拿真實資料查證後才動手。結果:**五條全部成立**,其中兩條比原本的說法更嚴重。

### ① 🔴🔴 現金比例:9 個受試者有 **6 個是錯的**
公開頁的「現金 X%」是前端用 `100 − 總曝險` **估**出來的,
而那條式子把**空頭部位當成消耗現金** —— 空單不是用現金買的。

| 受試者 | 頁面印的 | 帳本裡的真值 |
|---|---|---|
| **規則腦** | **現金 0%** | **50.0%** |
| Gemini | 40% | **70.0%** |
| GPT | 19% | 35.0% |
| Claude | 13% | 31.0% |
| 純淨腦 | 8% | 16.0% |
| 持久腦 | 16% | 28.0% |

規則腦被印成「現金 0%」(等於全押),而它實際握著**一半的現金** ——
這是對一個受試者風險姿態的**相反陳述**,就印在宣稱「公開每個決策」的頁面上。

**修法**:後端發佈 `current_cash_pct`(直接來自帳本),前端只讀不算。
🔴 拿不到時印「現金 未提供 ⚠️」,**不准退回去估** ——
估出來的數字看起來跟真的一模一樣,那才是最危險的。

### ② 資料日期只在最下面那張表的最後一列
2026-09-03 發佈鏈路故障、公開頁凍結 33.5 小時(CHANGELOG-010)時,
頁面看起來**完全正常** —— 唯一能看出來的地方是翻到表格最後一列。
**修法**:副標印「資料截至 YYYY-MM-DD」。新鮮度是讀者判斷要不要相信這一頁的前提。

### ③ 🔴🔴 「不隱藏違規與爆倉」**沒有任何東西可以支撐**
在此之前,公開資料裡**沒有任何欄位**會顯示爆倉、停牌折價或基建異常。
不是在藏事件 —— 而是那句宣稱**讀者無從檢查**。

逐日查證(46 個決策日):

| 事件 | 發生 | 頁面上原本 |
|---|---|---|
| 強制平倉(爆倉) | **0 / 46** | 無欄位 |
| 連續無報價 → 強制出場折價 | **0 / 46** | 無欄位 |
| **基建異常日(強制只減倉)** | **2 / 46**(2026-08-10、2026-09-01) | **真的發生過而一個字沒說** |
| 當日未能結算 | 0 / 46 | 無欄位 |

**修法**:新增「事件揭露」區塊,逐日從帳本算出天數**並印出分母**;
每日紀錄表上那兩天逐列標「⚙️基建異常」。
⭐ **「零」是有意義的揭露,但零一定要帶分母** ——
沒有分母的零,跟「這個欄位根本不存在」在畫面上長得一模一樣。

### ④ 持倉只印前 7 檔,而 GPT 當天有 **30 檔**
一個字都沒說被截斷 —— 跟 CHANGELOG(每日紀錄被截斷)是**同一個錯**。
**修法**:印「…只顯示前 7 檔,共 30 檔」。截斷本身不是罪,不講才是。

### ⑤ `fetch` 沒有 `.catch`
JSON 壞掉 / 404 / 讀到寫了一半的檔案時,整頁停在「載入中…」,
而「公開每個決策」「帳本不可竄改」那些文案**照常顯示**。
⭐ 「安靜地錯」的教科書形狀:**壞掉的時候看起來像正常的**。
**修法**:每一區印出「這一區沒有載入成功」,並明說
**「不要把空白當成沒有事件」**。

⚠️ 第一版的錯誤處理**漏了事件揭露區** —— 那一區會是空白的,
而空白就在「不隱藏爆倉」旁邊,讀起來正好是「沒有事件」。
實測對照組當場抓到。**列舉式的清單一定會漏一個。**

### 守衛
新增 8 條測試,七種突變逐一驗過。
⚠️ 其中三條第一版**逃掉了**(分母寫死 / 事件日期寫死 / 截斷提示永遠不顯示)——
因為我又在掃字串,而字串還在。改成量結構:
分母必須是 `len(...)`、每個事件欄位**各自**要有 `for h in hist`、
事件區塊裡不准出現寫死的日期、截斷條件必須就是 `_n>SHOW`。


---

## CHANGELOG-017 · 🔴🔴 撤回「重述數列」的承諾 —— 當初就不該許,而且現在做不出來

| | |
|---|---|
| 原承諾日 | **2026-08-03**(CHANGELOG-001) |
| 撤回日 | **2026-09-06**(欠了 34 天) |
| 性質 | **公開承諾未兌現**;經查證後判定為**當初不該許的承諾** |

### 原文(CHANGELOG-001)

> 修正後的重述數列尚未由本團隊獨立重跑驗證,因此本文件不列出任何重述後的數字。
> 重跑完成後將以 **CHANGELOG-006** 追加,並依 §9 分段呈現。

**兩件事都沒有兌現**:
1. CHANGELOG-006 那個編號**被別的條目佔用了**(曝險檢查的適用範圍未揭露)
2. 重述數列**從未交付**

### 🔴 為什麼是「做不出來」,不是「還沒做」

2026-09-06 實查帳本,忠實的重述需要五樣輸入,而我們**只留下一樣**:

| 重述需要的輸入 | 有沒有保存 |
|---|---|
| 每日各腦的配置(權重) | ✅ 有(`journal[].decisions[].alloc`) |
| 每日各腦的**部位數量** | 🔴 **沒有** |
| 每日各標的的**成交價** | 🔴 **沒有** |
| 每日各標的的**資金費率** | 🔴 **沒有**(唯一一份快照是 2026-08-17,在作廢期之後) |
| 每日的**成本拆解** | 🔴 **沒有**(逐日留痕是 CHANGELOG-009,2026-09-02 才加的) |

硬要做的話,只能重新去交易所抓歷史行情與費率、再反推部位數量 ——
那是一次**帶著新假設的重建(reconstruction)**,不是當初承諾的
「拿原始輸入重跑(replay)」。**兩者不是同一件事,結果也不會是同一個數字。**

### 🔴 而且「獨立驗證」這半句,本來就無法兌現

本專案只有一位維護者。「獨立重跑驗證」在合理讀者的理解裡是**第三方審查**,
而我們沒有第二個人。用另一個 AI 對話去說「我驗過了」不算獨立 ——
把「獨立」重新定義成那樣,是換一種方式食言。

⭐ **在一人公司裡承諾「獨立驗證」,是架構上就無法兌現的支票。**
回頭兌現承諾的唯一誠實方式,是承認當初開了這張支票,並註銷它。

### 撤回什麼、不撤回什麼

**撤回**:
- 「重跑完成後將以 CHANGELOG-006 追加重述數列」——**永久撤回,不再承諾交付**
- 「由本團隊獨立驗證」——**撤回**;本專案沒有能力提供第三方獨立驗證

**不撤回、繼續有效**:
- 🔴 **第 1~12 決策日的損益數字仍然不得用於任何對外績效陳述。** 這一條更嚴格地維持:
  以前是「等重述出來再說」,現在是「那段數字永久不可用於績效陳述」。
- 公開頁的大字報酬一律自 **2026-08-04** 起算,並印出基準權益供讀者驗算。
- 那 12 天的原始紀錄**保留在公開頁上**、逐列標示「作廢」,不刪除。

### ⚠️ 同時揭露一件我們原本沒說的事:**Epoch-2 的基準本身也受污染**

資金費被乘 3 倍會改變現金 → 改變權益 → 而部位大小是按權益百分比算的。
所以錯誤不只影響那 12 天的**報酬**,也影響了第 13 天**開始時的權益與部位**。

公開頁寫著「基準是作廢期最後一日的權益」——
那個基準值本身,就是用錯誤的資金費算出來的。

🔴 **我們無法量化這個污染有多大**(理由同上:原始輸入沒留)。
所以頁面上不會出現一個「校正後」的數字,只會把這件事講出來。

⭐ 這比原本的做法誠實:原本是把讀者的注意力導向「自 08-04 起算」,
  彷彿那條線之後就乾淨了。**那條線之後並不乾淨,只是比之前好。**

### 📌 已經做的防止再犯
- CHANGELOG-009(2026-09-02)起,每日**逐筆記錄換倉成本、換手金額、配置** ——
  正是因為這次發現「損益歸因做不出來,因為過程沒留」。
- CHANGELOG-013 起,PROTOCOL 有 §0 修訂紀錄,承諾與編號不會再互相衝突。
- 📌 **可複用的教訓**:承諾一件事之前先問「兌現它需要哪些輸入?我現在有在存嗎?」
  ——「以後再補算」在**沒有保存原始輸入**的情況下永遠是空頭支票。


---

## CHANGELOG-018 · 🔴🔴 §4 失格條件已有**四位競爭者觸發**,而頁面從未顯示

| | |
|---|---|
| 發現日 | 2026-09-06(續掃稽核未驗證項) |
| 揭露日 | 2026-09-06(**當日**) |
| 對帳本的影響 | **無**。四項都是「應揭露而未揭露」,帳本數字自始正確 |

### PROTOCOL §4 原文
> **風險上限(任一觸發即該腦當期失格)**
> - 最大回撤 > 35%
> - 發生強制平倉(爆倉)
> - 正式違規(重試後仍違反曝險/幣種約束)率 > 10% 的決策日

### 實查結果(46 個決策日)

| 競爭者 | 最大回撤(全期) | 最大回撤(自 08-04) | 正式違規 | §4 判定 |
|---|---|---|---|---|
| **規則腦** | **60.81%** | 30.81% | 0 / 46 | 🔴 觸發(全期回撤) |
| **等權多頭** | **55.38%** | **43.13%** | 0 / 46 | 🔴 觸發(兩個口徑都超) |
| **GPT** | 9.39% | 5.77% | **6 日 = 13.0%** | 🔴 觸發(違規率) |
| **純淨腦** | 7.58% | 5.23% | **9 日 = 19.6%** | 🔴 觸發(違規率) |
| Gemini | 23.76% | 20.44% | 0 / 46 | 未觸發 |
| Claude | 9.38% | 6.73% | 2 日 = 4.3% | 未觸發 |
| 持久腦 | 6.53% | 3.48% | 1 日 = 2.2% | 未觸發 |
| BTC 持有 | 6.06% | 4.39% | 0 / 46 | 未觸發 |
| 純現金 | 0% | 0% | 0 / 46 | 未觸發 |

🔴 **在 2026-09-06 之前,公開頁完全不顯示最大回撤、不顯示違規率、不顯示分母。**
唯一出現「失格」兩個字的地方,是行銷文案
「判定指標、**失格條件**、成本假設在看到任何結果之前就寫死」。

⭐ 這是「宣稱與事實不符」裡最嚴重的一種:
**不是算錯一個數字,是把一整條自己訂的規則藏起來。**

**修法**:新增「失格條件檢查(PROTOCOL §4)」表,逐腦列出三條判準的數值與分母。
🔴 門檻**從已錨定的 PROTOCOL 原文解析**,不寫死在頁面上 ——
寫死的話,協議改了而頁面還在用舊門檻,沒有人會發現;
而這一頁的賣點正是「判定標準跑之前就公開錨定」。
🔴 解析不到就印「(協議讀不到)」,**不准填一個我們記得的數字**。

**回撤兩個口徑都列**,不替讀者選一個:全期含已作廢的 12 日(那段損益本身是錯的),
Epoch-2 不含。規則腦只在全期超線、等權多頭兩個口徑都超。

⚠️ 觸發之後**不移除該競爭者、不停止記錄** —— PROTOCOL §9 禁止中途變更受試者名單。

---

## CHANGELOG-019 · 三處與資料或原文不符的敘述(同日修正)

### ① 🔴 持久腦的「抗擁擠」宣稱,被它自己的資料推翻
頁面原文:「持久腦…**當大家擠在一起時,它反而降曝險**。」

拿它自己 **45 個決策日**檢定(擁擠度 vs 當日總曝險):

| | 相關係數 | 最不擁擠 1/3 | 最擁擠 1/3 | 差 |
|---|---|---|---|---|
| **持久腦** | **+0.361** | 65.0% | 71.5% | **+6.5pp** |

**方向是相反的。** 已改成:設計目標是抗擁擠,而**目前的實測結果沒有支持它**。
⭐ 這正是這個實驗要量的東西 —— **不能拿設計目標當成果**。
(原句保留在頁面上,以「原本這裡寫『…』」的形式引用,不是刪掉了事。)

### ② 作廢原因引錯 CHANGELOG 編號
頁面說「資金費被**重複換算 3 倍**(CHANGELOG-**002**)」——
但「乘以 3」是 **CHANGELOG-001**;002 講的是「抓取分頁不足導致時間軸錯位」。
⭐ 引錯編號會讓想查證的讀者點進去看到不相干的內容,**那正是最傷「可查證」的地方**。
已改為同時引 001 與 002(兩者都影響那 12 天)。每日戰報的同一句也一併更正。

### ③ 權益走勢圖從 $10,000 畫起,而橫幅說「不列出含那 12 日的報酬」
圖上還畫著一條「$10,000 基準」虛線 —— **同一頁自己打自己**。
拿掉那段又會變成「靜靜丟掉紀錄」(CEO 2026-08-27 抓過的同一個錯),
所以**不刪、標出來**:作廢區段塗上警示底色並標註
「⚠️ 已作廢(12 個決策日,損益不得用於績效陳述)」。

### 守衛
新增 9 條測試。突變驗過:門檻寫死、失格表消失、所有人改成未觸發、
引錯編號、作廢區段不畫、拿掉 `i2>0` 檢查 —— 全部會紅。
⚠️ 其中兩條第一版是壞的:
- 「那句宣稱不准出現」→ 太粗,**它應該以引用形式留著**;改成量「有沒有被當成主張」
- 圖表守衛只掃 `fillRect` 兩個字 → 把繪製關掉照樣綠;改成釘區塊結構


---

## CHANGELOG-020 · 「算不出來」被寫成 0%,而我昨天做的守衛因此永遠不會亮

| | |
|---|---|
| 發現日 | 2026-09-06 | 揭露日 | 2026-09-06 |

`forward_multi._stats()` 在樣本不足時回 `{"total_pct": 0.0, "mdd_pct": 0.0}` ——
**把「算不出來」寫成「報酬 0%」**,而 0% 在畫面上會被讀成「這個腦不賺不賠」。

🔴 最諷刺的是:2026-09-05 我才在公開頁加了
「拿不到報酬就印『報酬未提供 ⚠️』」的守衛,而它只擋 `null` ——
**上游永遠給 0.0,所以那盞燈設計上永遠不會亮。**
(這是 `gate_denominator` 第十例的同型:判準恆綠。)

**修法**:`_stats` 與 `metrics` 的 fallback 改回 `None`;
每日戰報改成印「—」(戰報是給 CEO 看的,印假的 0.00% 會讓他以為那天平盤);
**內部儀表板**的 `? total_pct : 0` 一併修掉 ——
公開頁 09-05 修過,而內部頁原封不動,⭐ 又一次「同一個錯只修了一邊」。

---

## CHANGELOG-021 · 驗證器發佈了,但頁面上沒有任何入口

2026-09-05 把 `verify_chain.py` 隨鏈發佈到公開站,
但**頁面上沒有任何連結指向它** —— 讀者找不到,等於沒發佈。

同時,「✓ 帳本**不可竄改**」這種打包票的說法改成
「✓ 帳本**可驗證** —— 你可以自己驗」,並新增一張卡片給出:
1. 三行 `curl` + 一行 `python`,任何人可自行下載執行(只用標準庫)
2. 🔴 **它證明不了什麼**:雜湊鏈只防「改掉其中一筆」,防不了「整條重算」;
   防重算靠的是每日推 GitHub 的外部時間戳,那份證據在 git 歷史裡
3. ⚠️ **一件我們現在做不到的事**(外部顧問 Gemini 指出的信任斷層):
   驗證程式證明的是「**GitHub 上那份檔案**沒被改」,
   而讀者看的是**這個網頁** —— 網頁**不會**在瀏覽器裡即時比對雜湊。
   所以「你螢幕上的數字 = 被錨定的那一份」,**我們目前沒有給你證明**。
   這句話現在**寫在頁面上**,不是只存在我們心裡。

### ⚠️ 守衛本身的教訓:「取第一個命中」今天咬了我**四次**
協議解析器(§0 敘述句裡的舊數字)、副標測試(抓到 `d.error` 那行)、
持久腦宣稱、以及這一條的守衛(只檢查第一處「不可竄改」,
而第一處在合法的引述裡 → 把別處改回主張形式,測試照樣綠)。
📌 **判準要看每一處,不是隨便一處。**


---

## CHANGELOG-022 · 🔴 我剛印上頁面的「你可以自己驗」指令,照著做會**當機**

| | |
|---|---|
| 上線時間 | 2026-09-06 20:46 |
| 發現時間 | 2026-09-06 20:47(上線一分鐘後,自己照指令走一遍) |
| 修正時間 | 2026-09-06 20:52 |

CHANGELOG-021 才在頁面上放了「三行 curl + `python verify_chain.py`」。
我接著照那串指令自己走一遍 —— **第一次就當掉**:

```
UnicodeEncodeError
```

Windows 的預設 console 編碼(cp950 等)吐不出這支程式的中文與 emoji。
加 `-X utf8` 才過,而頁面上寫的是 `python verify_chain.py`。

🔴 **所有單元測試都是綠的** —— 因為 pytest 捕捉輸出,不走 console 編碼。
⭐ **「測試全綠」不等於「照文件做得起來」。要照使用者的步驟自己走一遍。**

**修法**:
1. 驗證器啟動時自己 `sys.stdout.reconfigure(encoding="utf-8", errors="replace")`。
   用 `replace` 不是 `ignore` —— 字印不出來要看到替代字元,
   靜靜消失的話讀者會以為那一行本來就沒有內容。
2. 新增測試:**用不給 UTF-8 的環境變數跑真的子行程**,確認不會 UnicodeEncodeError。
   突變(拿掉那行)會紅。

### 順帶:下載指令少抓三個檔
原指令只抓 `verify_chain.py` / `anchor_chain.json` / `dashboard_data.json`,
於是 `CHANGELOG.md` / `PROTOCOL.md` / `diaries.json` 顯示
「本機沒有這個檔案」—— 那是**沒有比對到**,不是「通過」。
已改成一次抓齊六個,並在頁面上寫明三種結果是分開的:
**通過 / 不符 / 沒有檔案可比對**。

## CHANGELOG-023 · 🔴🔴 我們把**自己的 API 斷線**公告成受試者「失格」

| | |
|---|---|
| 發現日 | 2026-09-19 | 揭露日 | 2026-09-19 |
| §9 分類 | 允許類別:「修補會造成錯誤估值的 bug」—— **不是修改判定指標** |

PROTOCOL §4 的原文是:

> 正式違規(**重試後仍違反曝險/幣種約束**)率 > 10% 的決策日

而 `_gate_check()` 直接拿 `len(violations)` 當分子。`violations` 裡混著一種
`no_response` —— 那是**我們打不到模型的 API**,它既沒違反曝險、也沒違反幣種。

**實測(分母 59 個決策日,門檻 10%)**:

| 腦 | 總違規 | 其中 `no_response` | 真違規 | 公告違規率 | 真實違規率 | §4 |
|---|---|---|---|---|---|---|
| GPT | 9 | **8** | 1 | **15.3%** | **1.7%** | 🔴 公告已失格 → 不該失格 |
| 純淨腦 | 12 | **8** | 4 | **20.3%** | **6.8%** | 🔴 公告已失格 → 不該失格 |

公開頁因此把兩個腦標成「🔴 已觸發:正式違規率」,旁邊還寫著
「這是**指令遵循能力**的真實量測」。

⭐⭐ **這是對外、對第三方模型的不實指控。假警報也是傷害** ——
而且方向特別糟:我方的基建中斷,被記成別人的紀律問題。

**修法**:違規分成三桶(`subject` / `infra` / `unknown`),§4 分子只取 `subject`。

📌 **刻意不用黑名單,也不用白名單** —— 兩種都會漏:
黑名單漏一個新的基建型別 → 又去誣指受試者;
白名單漏一個新的約束型別 → 真違規被靜默放行。
所以三桶全部明列,**落不進任何一桶的走 `unknown`,並讓 §4 判定變成
「⚠️無法判定」印在畫面上**(查不到 ≠ 沒有)。

**§9 合規**:原公告值 `violation_rate_pct_as_published` 保留並印在同一列,
「修改前後的樣本分開統計,不得混為一談」。**原始違規紀錄一筆未刪**,只改歸類。

**驗收**:新增 20 條測試;**6 個突變全部轉紅**(分子改回總數 / 未知型別倒向任一邊 /
拿掉無法判定旗標 / 連續天數改成總數 / 門檻改成 `>=`)。
失格數 4/9 → **2/9**(剩下規則腦與等權多頭,都是真的回撤超標)。
TG 戰報與每日戰報的同一個洞一併修掉 —— ⭐ 又一次「同一個錯只修了一邊」的預防。

---

## CHANGELOG-024 · 主指標的基準不是大盤,而這件事沒有寫在頁面上

| | |
|---|---|
| 發現日 | 2026-09-19 | 揭露日 | 2026-09-19 |
| §9 分類 | **不修改指標**;僅新增揭露 |

PROTOCOL §3 指定的唯一主指標是「相對 `bench_eqw`(等權多頭)的 Information Ratio」。
公開頁把它標成「🎯 主要比較基準」與「市場基準」,而**它不是大盤**:

`benchmark_allocation("bench_eqw")` 取 `candidates[:10]`,而候選表在
`multi.py:83` 按 `-abs(ret30d_pct)` 排序 = **近 30 日「絕對」漲跌幅最大的 10 支**,
再機械地全部做多、每日輪動。持倉年化波動中位數 **331%**(BTC 持有 43%)。

**後果(分母 46 個報酬日,Epoch-2)**:

| 基準 | IR 為正的競爭者 |
|---|---|
| 等權多頭(§3 主指標) | **8 / 8** |
| BTC 買進持有 | **2 / 8** |

用主指標這把尺,**純現金(完全空手)IR +0.97、規則腦(報酬 −4.4%,賠錢)IR +2.28**。

⭐⭐ **一個讓「什麼都不做」與「賠錢」都得正分的基準,量不到技術。**
(`gate_denominator` 第十二例:判準恆綠。)

**為什麼不換掉它**:§9 明文禁止中途修改 primary metric 與基準,而
「跑完發現指標不好看就換一把尺」正是這一頁承諾不做的事。
→ 依「**揭露但不阻擋**」:主指標原樣保留,**新增**同算法、換 BTC 基準的並列欄,
並在頁面上寫明那個「幾乎全部為正」不是好消息。

⚠️ **這不是全新發現**:根因早在 2026-08-25(CEO 問「等權多頭為什麼虧比較多」)
就寫進本檔,當時的答案是「它持有的幣波動 331%」。
**新的是它對主指標的後果** —— 那從來沒有人算過。

---

## CHANGELOG-025 · 「資料截至 <日期>」高估新鮮度約 23 小時

| | |
|---|---|
| 發現日 | 2026-09-19 | 揭露日 | 2026-09-19 |

排程每天 **00:36 UTC** 執行,`today = time.strftime("%Y-%m-%d", time.gmtime())`,
估值用 ticker 即時價。所以標記為第 D 天的那一列,量到的是
**[D−1 00:36Z → D 00:36Z]** 這個窗口 —— 其中 23 小時 25 分落在日曆日 D−1。

**量化**(把真實 BTC 日線位移 N 天後與 `bench_btc` 比對,58 天):

| 位移 | 平均絕對差 | >0.5pp 的天數 |
|---|---|---|
| 0 天 | 2.070pp | 49/58 |
| **1 天** | **0.338pp** | **12/58** |
| 2 天 | 1.867pp | 49/58 |

再用**分鐘 K** 以每天真實的錨點時刻重算窗口:Epoch-2 46 天平均絕對差
**0.056pp、最大 0.193pp、0/46 超過 0.5pp**。

🔴 **所以數字一個都沒算錯**(標 D 的淨值確實是 D 00:36Z 的真實淨值),
錯的只有「資料截至」這個**新鮮度宣稱**,而它沒有任何限定詞。
對立假說(「決策 D−1 做、持有到 D,所以標 D 是對的」)也量過:平均差 1.984pp,**被推翻**。

⭐ 讀者拿自己的 BTC 線圖對照會看到約 **6.7pp** 缺口,合理推論「連基準都對不上」。
**誤會的成本由我們承擔。**

**修法**:副標改印**快照時刻**(`anchor.utc`),並加一段說明
「最後一根完整日 K 是前一天的收盤,快照當天白天的行情還不在裡面」。

---

## CHANGELOG-026 · 載入失敗時的區塊清單,又漏了三個

`.catch` 裡那份手寫的區塊 id 清單,正上方的註解自己寫著
「**列舉式的清單一定會漏一個**」(CHANGELOG-014 漏掉 events 時寫的)——
然後它又漏了三個:新加的 `ircmp`、擁擠監控 `crowd`、權益圖 `chart`。

⭐ 再補一次清單只會再漏第四個。改成**結構性推導**:
fetch 之前先拍快照,記下「此刻還空著的、有 id 的容器」= 全部等資料填的區塊。
之後新增任何區塊都自動涵蓋,不需要有人記得回來改那一行。
(canvas 沒有 `innerHTML`,改插兄弟節點。)

---

## CHANGELOG-027 · 🔴 GPT 與純淨腦停擺三天,而系統說不出為什麼

| | |
|---|---|
| 發生日 | 2026-09-17 ~ 09-19(連續 3 個決策日) | 查明日 | 2026-09-19 |
| 影響 | 5 個 LLM 腦有 **2 個**沒有做決策;頁面顯示的是它們 09-16 的舊倉位 |

**真正的原因**:

```
HTTP 429  insufficient_quota
"You have no credits remaining."
```

OpenAI 帳戶額度用盡。**與模型本身無關。**

🔴 而系統整整三天說不出這件事,因為 `sliceb/llm_router._post` 是
`except Exception: return None` —— **錯誤被整個吞掉**,
帳本只留得下 `no_response` 三個字。
⭐⭐ **一個說不出自己為什麼失敗的呼叫,會把五分鐘的故障拖成三天。**
而這三天裡,公開頁還把它公告成那兩個腦「失格」(見 CHANGELOG-023)。

**修法**(行為刻意不變:照樣回 `None`,呼叫端照樣退模板):
1. `_post` 記下 HTTP 狀態碼與供應商回的原因。
2. 🔴 **遮蔽金鑰**:Gemini 把金鑰放在 **URL 查詢字串**裡,而錯誤訊息會進日誌與
   帳本(帳本還會推上公開 repo)。`sk-` / `AIza` / `xoxb-` 樣式一律遮蔽,
   但**不得遮過頭** —— 洗掉診斷資訊等於沒修(善:假警報與無用警報都是傷害)。
3. 每日作業把原因寫進帳本 `infra_failure_reasons`,跟著那筆 `no_response` 走。

**驗收**:9 條測試;**7 個突變全部轉紅**(退回吞錯 / 成功不清舊錯 / 拿掉遮蔽 /
遮蔽過頭 / 拿掉長度上限 / 不記例外型別 / 原因不寫進帳本)。
⭐ 第一版的假金鑰寫成字面值,被既有的 `test_testpack` 金鑰掃描器當場抓到 ——
**那個守衛是對的**,測試包裡不該出現金鑰形狀的字串,即使是假的。已改成執行時才組。

⏳ **額度要由 CEO 處理(會花錢)。** 在那之前這兩個腦會繼續維持原倉,
公開頁會照實標示「已連續 N 個決策日沒有新決策」。

---

## CHANGELOG-028 · 壞掉的時候不准看起來像正常(四件同形狀的事)

| | |
|---|---|
| 發現日 | 2026-09-20 | 揭露日 | 2026-09-20 |

四件事共用同一個形狀:**畫面上「沒事」與「查不到」長得一模一樣。**

### ① §4 門檻讀不到 → 整張表全綠
門檻要從已錨定的 PROTOCOL 原文即時解析。解析失敗時 `thresholds` 是 `None`,
於是每一個腦的 `triggered` 都是空陣列 → 全部印綠色「未觸發」,
頁腳印綠色「**0 / 9** 位競爭者已觸發」。
**實測**(把 PROTOCOL 讀成 None):觸發數 2/9 → **0/9 全綠**。
⭐ **讀不到規則 ≠ 沒有人違規。**(`gate_denominator` 第十三例。)
→ 門檻缺任何一條就不印綠色結論,改印「⚠️ 無法判定(門檻讀不到)」+ 警示框。

### ② 發佈停擺與正常運作長得一樣
這一頁**從來沒有任何一行程式**比對「資料日期」與「現在」。
2026-09-02 公開頁凍了 33.5 小時,畫面完全正常(CHANGELOG-010)。
→ 加上在**讀者瀏覽器裡**跑的新鮮度檢查:我們的伺服器整個掛掉時它照樣會說話。
🔴 門檻設 **40 小時**(排程每 24h 一次),留足重跑與時區餘裕 ——
**會誤報的紅燈遲早被當成背景雜訊**,那比沒有更糟。

### ③ 創世日期配上現行協議的雜湊
副標印「預先登記已外部錨定 **2026-07-22**(PROTOCOL **1b953e4d**)」。
而 `1b953e4d` 是 **seq 48(2026-09-06)** 才上鏈的;2026-07-22 那天錨的是 `48973c29`。
⭐ **日期配了一個當時還不存在的雜湊**,而這一頁的招牌正是「事後不能換指標說故事」。
→ 兩個日期都印:「創世錨定 2026-07-22(當時的 PROTOCOL 48973c29…)｜
現行 PROTOCOL 1b953e4d… 自 2026-09-06 上鏈」,並在資料檔附版本沿革。
⚠️ 順手修掉自己剛寫的 bug:版本去重拿**截斷後 16 字元**比**完整 64 字元** →
永遠不相等 → 62 筆全被當成「換過版」。**比對要用同一個口徑。**

### ④ 擁擠度沒有分母,而且含過期持倉
「最新思考腦組合重疊度 42.8%」只印一個百分比:沒說它是由 **3 個模型家族**算出、
沒說 59 天裡只顯示最新 1 天。
🔴 更要命的是 2026-09-17~19 GPT 與純淨腦沒有回應,它們的「持倉」是三天前的舊倉 ——
**重疊度等於拿過期資料去算**,而畫面上看不出來。
→ 印出分母、印出「共 N 天只顯示 1 天」、並在有腦使用過期持倉時明白標示。

### 順帶:Sharpe 的宣稱與行為不一致
舊文案「樣本數不足 20 日時不顯示 Sharpe」**暗示樣本夠了就會顯示** ——
而樣本早就夠了(47 ≥ 20),這一頁卻從頭到尾沒顯示過。
→ 改成講清楚**我們刻意不放**(§3 次要指標;幾十天樣本上會算出 4~5 這種
好看但沒意義的數字),同時告訴讀者 Sharpe 與 stability **都在公開資料檔裡**。
⭐ 「不顯示」與「不給」是兩回事。

### 🔴 一條稽核指控**不成立**,已駁回
外部稽核報「§4『發生爆倉』欄設計上不可能亮紅,真爆倉那天反而會讓整份 JSON 停更」。
**實測駁回**:`liquidations` 欄 59/59 天都在;注入一次爆倉後該欄變 1、
判定轉成「🔴 已觸發:發生爆倉」、`build()` 也正常完成。**兩半都不成立,未照它修改。**

**驗收**:9 條新測試;**13 個突變全部轉紅**;瀏覽器反向注入實測
(門檻設 None → 全列「無法判定」、頁腳不再印綠色 0/9;快照改 3 天前 → 跳出
「已經 95 小時沒有更新」;資料正常時**不誤報**)。全套 3,705 → **3,714**。

---

## CHANGELOG-029 · 三處「查不到被印成 0」的收尾

| | |
|---|---|
| 發現日 | 2026-09-20 | 揭露日 | 2026-09-20 |

### ① §4 表對**從未受檢查**的競爭者印「0 日 / 0%」
曝險/幣種檢查寫在 `multi._parse_alloc()` 裡,而它**只被 `llm_allocation()` 呼叫** ——
規則腦與三個市場基準從來不經過它。資料檔裡早就有 `violation_checked` 欄位,
而這一頁 **一次都沒讀過**。於是那 4 位在表上顯示「0 日 / 0%」,讀起來像「最守規矩」。
實測規則腦 2026-07-25 的實際總曝險曾達 **174.6%**。
📌 「適用範圍」的說明原本在**兩張卡片之前**的紀律紀錄區 ——
⭐ **揭露要放在資料旁邊,不是另一個區塊**(這是第四次犯)。
→ 那一格改印「未受檢查」,說明搬到 §4 表自己的頁腳。

### ② 缺漏日落在換頁邊界時,兩頁都不會報
缺日偵測只掃**當前這一頁**的日期區間。若缺漏日正好在換頁邊界,
兩頁的區間都不含它 → **兩頁都沒有警示**。
**實測**:刪掉排序後索引 30 那天(邊界),修正前兩頁皆無警示;
修正後**兩頁都報出來**。
⭐ 「少一天也是不見」,而分頁讓它更容易不見。
→ 改成掃整個資料集,並把本頁以外的缺漏也一併告訴讀者。

### ③ 規則腦現金印成「-0%」
它的現金是 **−0.1%**(真的略為透支),而 `toFixed(0)` 產生字串 `"-0"`。
四捨五入成 `0%` 也是錯的 —— **它不是零**。
⭐ **抹平就是把「略為透支」講成「剛好打平」。**
→ |x| < 0.5% 時多給一位小數;真正的零仍印 0。

**驗收**:4 條新測試;**突變逐條轉紅**(含兩條第一版抓不到、由突變測試當場補強的:
`checked` 改成常數、兩句缺漏說明用 `or` 串接)。全套 3,714 → **3,720**。

### 📌 這一天「取第一個命中」咬了**九次**
其中兩次是測試命中**我自己的註解**(註解正好引用了頁面文字),
一次是紀律紀錄區有幾乎一模一樣的句子。
⭐ **掃字串的測試量到的是我的文案,不是頁面的行為 —— 要先剝註解,而且要看每一處。**

---

## CHANGELOG-030 · ✅ 充值後驗證:兩個腦恢復,而今天記到的是**真違規**

2026-09-20 00:36 UTC 的排程(day 60, seq 62)跑完:

| 腦 | 結果 |
|---|---|
| GPT | ✅ 正常做決策 |
| 純淨腦 | 🔴 `gross_exceeded:109%` —— **真的超額配置**,不是 API 斷線 |
| Gemini / Claude / 持久腦 | ✅ 正常 |

`no_decision` 全部歸零,`infra_failure_reasons` 沒有新增。
⭐ 這一天剛好示範了 CHANGELOG-023 分類的用處:**API 通了,所以今天記到的是
受試者自己的違規(進 §4 分子);而前 8 次斷線不進**。
純淨腦累計受試者違規 4 → 5,違規率 8.3%,仍低於 10% 門檻。

---

## CHANGELOG-031 · 「已修正」不可以是一句永遠推翻不了的話

| | |
|---|---|
| 發現日 | 2026-09-20 | 揭露日 | 2026-09-20 |

更正聲明裡那句:

> 另有帳本與淨值序列不一致 **22** 筆(已於 2026-07-26 修正)。

**筆數是即時算的,「已修正」是寫死的。**
今天查證它剛好為真(22 筆全部落在 2026-07-23 ~ 07-25,都早於修正日)——
但明天若出現新的不一致,頁面會印「25 筆(已於 2026-07-26 修正)」,
而**沒有任何機制會發現那句話已經變成假的**。

⭐ **一句永遠不會被推翻的「已修正」,跟沒有揭露是一樣的。**

**修法**:後端加 `returns_reconcile_range`(first / last / fixed_on / after_fix),
頁面改成從資料推導 ——
- `after_fix == 0` → 印出實際日期範圍 +「該日之後 **0** 筆(這個 0 是每天重算的)」
- `after_fix > 0` → 改口「**其中 N 筆發生在宣稱修正日之後 —— 那個 bug 沒有真的修乾淨**」

修正日也從頁面搬到 `forward_multi.RECONCILE_FIXED_ON` 單一來源,頁面不再寫死。
**驗收**:瀏覽器注入 `after_fix=3` → 頁面當場改口(實測);突變(條件改成 `false`
或換成別的欄位)兩種都轉紅。

### 順帶:原始碼對自己的上線狀態說謊
`lab.html` 裡有兩處註解掛著「⏳ 未提交 · 等 CEO 決定」,
而那兩段(作廢橫幅、各腦卡片)**早就上線了** —— 線上看得到。
⭐ 以後要找「還沒上的東西」時,這種假陽性會把真的那一筆淹掉。
→ 標記已更正;新增守衛:**已上線的檔案裡不准出現「⏳ 未提交」**。

### 🔴 內部儀表板:同一個錯只修了一邊,第三次
`shadow/dashboard/index.html` 仍用 `100 − 總曝險` 估現金 ——
**那正是公開頁 2026-09-06 修掉的 bug**(當時實測 9 個腦錯 6 個;
規則腦畫面印「現金 0%」而帳本是 **50%**)。
而它上面三行的註解自己寫著「又一次同一個錯只修了一邊」。
內部頁同時也不顯示 `today_violation` —— 被駁回的原案會被當成它執行的決策。
→ 兩者都改;守衛改為 **parametrize 過兩頁**,想只改一邊過不了。

---

## CHANGELOG-032 · 複查 2026-09-05 稽核:三條確認仍然存在

2026-09-05 的七維度稽核共 **102 條**(40 高 / 49 中 / 13 低)。
⚠️ 那份清單原本**只活在對話紀錄裡** —— 2026-09-20 要複查時是靠翻 workflow
journal 才找回來的。**能找回來是運氣。**
→ 已落地成 `docs/稽核/20260905_公開面稽核_102條.json`(含逐條狀態欄)。
⭐ **沒有被存成檔案的清單,等於沒有分母。**

### A032 🔴🔴 帳本讀不到時,會把線上資料**洗成一個錯誤物件**並回報成功
```python
data = build()                      # 找不到帳本 → {"error": "尚無實驗資料"}
json.dump(data, open(OUT, "w"))     # 🔴 把線上 60 天資料整份覆蓋掉
print(f"...已更新:{data.get('meta',{}).get('days')} 日")   # 印「已更新:None 日」
```
⭐⭐ **三件事同時錯**:破壞性覆寫 + 謊報成功 + 回傳碼說沒事。
排程看到 `exit 0` 會繼續往下跑錨定,**把那份錯誤物件推上公開站**。

**修法**:算不出東西就**什麼都不寫**、印到 stderr、回非零,並在進入點 `sys.exit`。
🔴 「不寫」比「寫錯誤物件」安全:舊資料至少**曾經是對的**,
而頁面自己的新鮮度檢查會指出它過期(那是可判定的);錯誤物件則是**新的假資料**。
🔴 判準不用黑名單:**任何沒有 `meta` 的產出**都擋,不是只擋 `error` 這個 key。
**驗收**:實際把帳本改名跑一次 → `exit 1`、輸出檔**未被覆寫**;6 個突變全部轉紅。

### A038 🔴 決策日記只發佈最後 10 天,**靜靜截掉 50 天**
`diaries.py` 的 `journal[-10:]` —— 而公開頁掛著「✓ 公開每個決策與理由」。
⭐ 這和 CEO 2026-08-27 抓到的是**同一個 `[-N:]`**
(「不要前面就不見了,持續積累下來」)。當時修的是 `emit_dashboard`,
而守衛 `test_儀表板不准對每日紀錄設上限` **只掃 `emit_dashboard.py`** ——
這一支從來不在分母裡,所以它安靜地截斷了一個月。
(`gate_denominator` 同型:**判準的分母只放了我修過的那份檔案**。)
→ 全部發佈(60 天 = 0.29 MB);守衛擴及 `diaries.py`,並加一條
「發佈筆數必須等於 total_days」的交叉比對。

### A024 ⚠️ 缺漏日的隔天那一列是 **48 小時**的報酬
2026-07-28 沒有紀錄 → **2026-07-29 那一列實際涵蓋 07-27 → 07-29**。
頁面報了缺漏日,卻沒說那一列跨兩天。
🔴 **查證後下修嚴重度**:它落在已作廢的 12 天內,而
**Epoch-2 的 48 列跨距全部等於 1 天(實測)** —— 有效樣本是乾淨的。
→ 仍然揭露,但由**資料逐列算**:跨距 >1 天的列一律列出,
並分辨「全在作廢期內(目前)」與「有列不在作廢期內(會影響波動度)」。
**驗收**:注入一個有效期內的缺漏 → 頁面立刻改口
「🔴 其中 1 列不在作廢期內 …會影響波動度與相關的風險指標」。

---

## CHANGELOG-033 · 協議指名要錨的東西,鏈沒有蓋到

| | |
|---|---|
| 發現日 | 2026-09-20(稽核 A006 複查) | 生效日 | 2026-09-21 錨定起 |
| §9 分類 | 允許類別:「修補會造成錯誤估值的 bug」—— **擴大保護範圍,不改任何判定** |

PROTOCOL §14 原文要求錨定:

> PROTOCOL 全文與 hash、程式碼檔案 hash、**模型與參數清單**、初始帳戶與持倉、統計規格。

而 `models` / `llm_params` / `cost_assumptions` **只存在於 `anchor.json`**,
那個檔案**不在** `PUBLISHED_FILES` —— 也就是不在每日雜湊的範圍內。
(在其他四個受保護檔裡 grep 這些模型名稱,全部零命中。)

**實測(竄改測試)**:把公開的 `anchor.json` 改成
`start_capital=5000`、`models={"gpt":"FAKE-MODEL"}`、`declaration="TAMPERED"`,
**驗證器輸出一字不差** —— 它從頭到尾沒有提過這個檔案。

⭐ 這不是顯示錯誤,是**協議自己承諾要錨、而鏈沒有蓋到**的保護範圍缺口。

**修法**:`anchor.json` 加入 `PUBLISHED_FILES`。
- 順序安全:它在 `anchor()` 裡先寫出,`published_hashes()` 之後才取雜湊(實查行號)
- 無自我指涉:頂層 11 個 key 沒有 seq/prev/chain
- 舊鏈不受影響:每筆 entry 帶自己的 `published` dict
- 🔴 同時自動納入換行正規化 —— 乾跑證實它**本來就是 CRLF**,
  不正規化會重演 CHANGELOG-015 的「驗證器誣指我們自己竄改」

**驗收**:沙盒乾跑 `anchor(push=False)` → seq 63 的 `published` 含 `anchor.json`;
**竄改測試反向對照**:改前驗證器對它一字不提,改後印
`✗ anchor.json 鏈上 88304cfd… 現在 ed936004…`。2 個突變轉紅。

⚠️ **誠實補充**:這份檔案目前沒有從公開頁連出去,所以讀者被誤導的實際機率低;
修的是「宣稱的保護範圍 < 協議承諾的範圍」。

---

## CHANGELOG-034 · 同一頁上作廢天數 12 與 13 打架

| | |
|---|---|
| 發現日 | 2026-09-20(稽核 A016/A036 複查) | 揭露日 | 2026-09-20 |

權益圖左上角的標籤印「⚠️ 已作廢(**13** 個決策日)」,
而同一頁的橫幅、每日紀錄表、`meta.voided_days` 都是 **12**。

根因:標籤印的是 `i2` —— `epoch2_first_day` 在**曲線陣列**裡的**索引**。
而曲線的第 0 點是合成的「起始」點、**不是決策日**,所以索引比天數多 1。

⭐ **把索引當成天數用。** 而這一頁的招牌正是「可查證」——
一個真的想對帳的讀者,第一眼就會撞到這個矛盾。

**修法**:天數只能有一份權威(後端 `meta.voided_days`);
索引仍用來決定**畫到哪裡**(那是位置,不是天數)—— 兩件事不再共用同一個數。
拿不到時印「天數未提供」,**不准退回索引**。

**驗收**:攔截 canvas `fillText` 實測標籤 = 「⚠️ 已作廢(12 個決策日…)」,與 meta 一致;
4 個突變全部轉紅(含「把 `const vd` 的來源換成 `i2`」這種只改來源不改變數名的)。

---

## CHANGELOG-035 · 🔴🔴 真的爆倉那天,整份公開資料會停止更新

| | |
|---|---|
| 發現日 | 2026-09-20(稽核 A031 複查) | 揭露日 | 2026-09-20 |

### ⚠️ 先更正我自己:2026-09-19 我「駁回」過這一條,那個駁回是錯的
當時我注入的是 **dict 形狀的 `liquidations` 標記**,跑起來一切正常,
於是判定「稽核兩半都不成立」並寫進 CHANGELOG-028。
**我測錯了機制。** 真正的兩條路徑都沒被碰到。
⭐ **測錯機制的反證,不是反證。**

### 兩個 bug 疊在同一個情境上

**①「歸零後繼續」會 `ZeroDivisionError`**
`_stats` 算 `eqs[i]/eqs[i-1]`。真實的爆倉形狀是
「今天歸零、**明天還是 0**」—— 所以會爆的不是爆倉當天,是**隔天**。

**② `liquidations` 的形狀讀錯了**
帳本存的是 **list of `{brain, reason, ...}`**
(`forward_multi.py:404,438,457,466`),
而 `_gate_check` 寫 `(h.get("liquidations") or {}).get(b)` —— 當成 dict。
空 list 時 `or {}` 剛好接住,所以 60 天來一直「正常」;
**一旦真的有一筆紀錄,`.get()` 就 AttributeError。**

**實測**:注入「某腦連兩天權益 0 + 一筆真實形狀的爆倉紀錄」→
`metrics()` 與 `emit_dashboard.build()` **兩條路徑都拋例外**。

⭐⭐ 合起來:§4「發生爆倉」那一欄不只是「不會紅」——
**它會在該紅的那一天讓整份公開資料停止更新**(不更新、不錨定、不備份)。

### 修法
- 🔴 **從 0 起算的報酬是「未定義」,不是 0**。略過它們,但把略過幾筆
  (`undefined_returns`)**數出來送給下游** —— 靜靜吞掉就是另一種說謊。
- 起點為 0 時全期報酬回 `None`,不准無中生有一個數字。
- 回撤只在 `peak > 0` 時才算。
- `_liq_days()` 認 list **與** 舊 dict 兩種形狀。

**驗收**:四種情境實跑 `build()` 全部完成且爆倉欄正確
(list 形狀 1 / 舊 dict 形狀 1 / 沒爆倉 0 / §4 正確觸發「發生爆倉」);
13 條測試;**6 個突變全部轉紅**(含「_liq_days 永遠回 1」這種退化組)。

📌 這一份的守衛在掃原始碼時**先用 `ast` 剝掉 docstring** ——
第一版紅在自己的說明文字上(我在 docstring 裡引用了那個壞寫法)。
⭐ **掃字串的測試量到的是我的文案,不是行為。**

---

## CHANGELOG-036 · 拿一把已知會低估的尺,量出低值,再用綠色說安全

| | |
|---|---|
| 發現日 | 2026-09-20(稽核 A013/A027 複查) | 揭露日 | 2026-09-20 |

「🚨 擁擠監控」印一個綠色百分比 + 「跨家族組合分散=較健康」。
而**本檔「已知待處理」第 2 項**早就寫著(且**沒有刪除線** = 未解決):

> **擁擠度門檻無依據**:`multi.py` 的 `avg >= 60` 為硬編碼,程式與 PROTOCOL 皆無推導。
> …**持倉重疊度會系統性低估真實擁擠。**
> 目前公開頁對 19.0% 標示「✅ 分散」,此一保證缺乏依據。

⭐ **拿一把已知會低估的尺量出低值,再用綠色告訴讀者安全** ——
而這張卡的標題正是「🚨 擁擠監控」、副標寫「這是特色不是 bug」。

### 實測(重新量,不引用舊值)
當時那個 +0.72 是 **n=12** 的數字。以 Epoch-2 **48 個有效日、10 對**重算:

| | |
|---|---|
| 持倉重疊度 | 平均 **32.3%**(12.3% ~ 48.8%) |
| **日報酬相關係數** | 平均 **+0.539**,最高一對 **+0.833**(gpt vs 純淨腦) |
| 60 天中觸發警訊(≥60%)的 | **1 天**(2026-07-23) |

**持倉看起來只重疊 35%,報酬卻明顯同向。** 這就是那把尺低估的量級。

### 修法(不改指標,改揭露 + 把另一把尺也擺出來)
1. 🔴 **把報酬相關係數算出來擺旁邊**:重疊度看「持倉像不像」,
   相關係數看「**賺賠有沒有一起動**」—— 後者才是擁擠真正會傷人的地方。
   逐日從公開報酬算,**不寫死**(寫死就會變成另一個過期的假數字)。
2. 🔴 **把門檻是誰定的寫出來**:`CROWDING_WARN_PCT = 60.0`,
   來源「硬編碼於 `shadow/multi.py`,PROTOCOL 未定義」。
   對照:§4 的失格門檻是從**已錨定的協議原文即時解析**的 ——
   ⭐ **兩條線在畫面上都像「規則」,但只有一條真的被錨定過。**
3. 顯示 60 天的範圍與**觸發過警訊的那一天**,不再只印今天(A027)。
4. 後端沒給相關係數時明講「**空白不等於不相關**」。
5. 新產生的 note 改成事實陳述「跨家族持倉重疊度不高」,不再下健康判斷。
   (歷史資料不改寫 —— 舊 note 留著,揭露就緊貼在它下面。)

**驗收**:6 個突變全部轉紅;瀏覽器反向注入(移除 `crowding_reality`)
→ 頁面改印「空白不等於不相關」,不是默默消失。

📌 順帶修掉我自己測試的一個老毛病:三條判準用**固定長度視窗** `h[i:i+1800]`,
區塊一變長就掃不到後半段 —— ⭐ **固定長度的視窗是會過期的判準**,已改成錨在區塊邊界。

---

## CHANGELOG-037 · 進度表本身會過期,而且編號有兩套

| | |
|---|---|
| 發現日 | 2026-09-21(CEO 問「有沒有重工?」之後自查) | 揭露日 | 2026-09-21 |

### ① 落地隔天就過期
2026-09-20 把 102 條稽核清單落地成 `docs/稽核/20260905_公開面稽核_102條.json`,
寫上逐條狀態。**然後繼續修東西,卻沒有回頭更新那個檔。**
隔天一查:**四條已經修好的,檔案裡還寫著 `still_open`。**

⭐⭐ **一份會過期的進度表,比沒有進度表更糟** ——
下一個讀它的人會去重做已經做完的事,而真正沒做的那幾條被淹在假的待辦裡。

### ② 🔴🔴 兩套編號並存,CHANGELOG 的引用 8/9 指錯人
落地時用「第 N 條(**不分嚴重度**)」編號,
而 CHANGELOG-031~036 引用的是「第 N 條**高嚴重度**」。
實查 9 個引用:**除了 `A006` 是巧合,其餘全部指向完全不同的條目。**

例:CHANGELOG 寫「稽核 A013 擁擠監控」,而追蹤檔的 A013 是
「CEO 抓到的那個截斷可以原封不動改回去」—— 完全不相干的兩件事。

⭐ **兩套編號並存 = 遲早指錯人,而且指錯的時候完全看不出來**
(兩邊都真的有一條叫 A013 的東西)。

**修法**:CHANGELOG 是 append-only(§9:「不修改既有內容」),
所以**改追蹤檔去對齊它**,不能反過來。
編號改成帶嚴重度前綴:**A**=high(40)/ **M**=medium(49)/ **L**=low(13),
各自從 001 編起 —— 讓兩套編號在結構上不可能再混。

### 新增守衛(10 條)
- 下了結論就必須留**證據**與**日期/人**(「已修」不能只是一個標籤)
- 檔頭統計必須跟內容對得上(手寫的統計一定會漂)
- 編號前綴必須對應嚴重度、各自連續
- 🔴 **CHANGELOG 引用的每個編號都必須在追蹤檔裡找得到人**
- ⭐ 一條**永遠會過**的測試,把目前進度印在每次測試輸出裡 ——
  **過期不該靠「有人記得去看那個檔案」才被發現。**

### 目前進度(這個數字現在由測試每次印出來)
高嚴重度 40 條:**31 已修 / 5 部分修 / 4 仍然存在**
(4 條中有 2 條屬山寨雷達頁,不在本專案範圍)
中低 62 條:**全部尚未複查**

---

## CHANGELOG-038 · 「指令遵循能力」量了 61 天,而讀者看不到

| | |
|---|---|
| 發現日 | 2026-09-21(稽核 A020/A022 複查) | 揭露日 | 2026-09-21 |

### A022 🔴 紀律紀錄表宣稱量「指令遵循能力」,卻只給今天
那張表唯一有的欄位是「**今日**首次違規」—— 永遠是 0 或 1,
**五個腦看起來全都一樣**。

⭐ 「首次違規率」才是那句宣稱要量的東西:模型**第一次就把規則寫對**的比例。
重試後的違規只量「改不改得回來」,那是另一回事。

🔴 而累計清單**一直好好躺在帳本裡** —— 不是量不到,是發佈時被過濾成只剩今天。

**攤開之後才看得到差別**(61 個決策日):

| 腦 | 首次就違規 | 比率 |
|---|---|---|
| 純淨腦 | 14 次 | **23.0%** |
| GPT | 5 次 | 8.2% |
| 持久腦 | 4 次 | 6.6% |
| Claude | 3 次 | 4.9% |
| Gemini | **0 次** | **0.0%** |

舊表上這五個全都顯示「今日首次違規:0」。

#### ⚠️ 順帶:我第一版自己造了一個新洞
`first_attempt_cumulative` 照抄帳本那個 dict,而**帳本只在某腦真的違規時才建 key**
—— 所以 Gemini(唯一一個 0 次的)在畫面上印「**—**」,看起來像沒資料,
而其他表現較差的腦都印得出數字。
⭐⭐ **表現最好的那一個看起來像壞掉** ——
這是「查不到 vs 沒有」搞混的**反向版本**,而且它剛好懲罰了做對的人。
→ 分母改成含**每一個受檢查的腦**。

### A020 🔴 「共 X 天」從來沒跟 `meta.days` 對照過
`records` 被部分截斷時,分頁列會印一個比真實小的數字,
而同一頁其他每一個分母(副標、事件表、擁擠度)**還是對的** ——
⭐ **一頁之內兩個分母不一致,而沒有任何一行程式會發現。**
舊的防線只擋得住 `rec.length === 0`(全空),擋不住少了 5 天。

→ 前端比對並印紅字;**後端守衛 parametrize 過兩份資料檔**
(原本只量內部那份 —— `gate_denominator` 同型:判準的分母只放了我這邊那一份),
缺檔改成 `assert` 不再 `skip`。
**驗收**:瀏覽器注入砍掉 5 筆 → 立刻印
「後端說 61 天,這裡只有 56 天 —— 少了 5 天,不要把這張表當成完整紀錄」。

### 順帶:頁面上有沒被渲染的 markdown
§4 標題區的「門檻\*\*從已錨定的 PROTOCOL 原文解析\*\*」已經這樣上線一段時間 ——
HTML 不吃 markdown,讀者看到的是兩個星號。
新增守衛:**剝掉註解後**,輸出字串裡不准出現 `**…**`。
⭐ 註解裡怎麼寫都行,**輸出字串裡不行**。

**驗收**:13 條新測試;**7 個突變全部轉紅**(含我今天重複犯的三種:
`false` 架空條件、兩句說明用 `or` 串接、根本沒有測試涵蓋)。
全套 3,786 通過。

---

## CHANGELOG-039 · 🔴🔴 錨定排程會替別人按下發佈鍵

| | |
|---|---|
| 發現日 | 2026-09-21(稽核 M034 複查) | 揭露日 | 2026-09-21 |
| 性質 | **這不是顯示問題,是發佈控制破口** |

`landing-page` 是**公開網站**,而且有**多個寫入者**(CEO、山寨雷達視窗、影子實驗)。
錨定排程每天 00:36 UTC 跑:

```
git add data/shadow → git commit → git push
```

**沒有任何範圍檢查。**
任何人在本機 commit 了定價頁、文章、草稿等著審核,
**排程就會替他按下發佈鍵** —— 而且沒有人會收到通知。

⭐ 只檢查暫存區是擋不住的:`git push` 送的是**整條分支**,
包含別人先前 commit 但**還沒推**的東西。

### 修法:移植,不是新寫
`山寨雷達預報/engine/publish_public.py:546-623` 為了**同一個情境**
早就寫好並驗證過三道守衛。📌 **先查既有資產再動手。**
這裡補成四道(多一道是因為這支有 rebase 重試):

| | 時機 | 擋什麼 |
|---|---|---|
| A | push 前 | 本機已 commit 但未推的東西裡有沒有範圍外檔案 |
| B | `add` 後 | 暫存清單有沒有範圍外檔案(只退我們自己 add 的) |
| C | commit 後 | 這次真的要送出去的 commit 動了哪些檔 |
| D | **rebase 後** | rebase 會把本機 commit 重疊上去,可能冒出新的範圍外檔案 |

🔴 **fail-closed 的取捨**:寧可當天不錨定,也不要誤發佈。
錨定漏一天補得回來(鏈會記錄、公開頁的 40 小時新鮮度檢查會說話);
**誤發佈的內容收不回來。**

### 🔴 順帶抓到一個讓這整段「測不到」的缺陷
```python
def _git(*args, cwd=REPO):     # ← 預設參數在 def 時就求值
```
`REPO` 被綁死成模組載入那一刻的值,**任何測試想把它指向沙盒都無效** ——
它會安安靜靜地去操作**真正的公開 repo**。

我自己就踩到:守衛寫好後做沙盒驗證,結果 `pushed=True` 而沙盒遠端什麼都沒收到。
查下去才發現它在對真 repo 做 no-op push。
**幸好那次是 no-op —— 但那是運氣,不是設計。**
(已確認真 repo 未受影響:工作區乾淨、無多餘 commit、遠端內容正確。)

⭐⭐ **測不到 = 沒有守衛。** 整個發佈路徑(這個專案最危險的一段)
在此之前**零測試覆蓋** —— 正是 `gate_denominator` 那一類:
**判準的分母裡沒有它。**

**驗收**:用**真的 git repo**(bare remote + work tree)做沙盒:
正常情況照推、遠端收到 `anchor_chain.json`;
注入一筆含 `pricing.html` 的未推送 commit → `pushed=False`、
訊息指名該檔、**遠端完全沒有它**。
6 條測試;**7 個突變全部轉紅**(含「範圍放寬成整個 data/」
與「`data/shadow` 沒帶結尾斜線會誤放 `data/shadowx`」這種邊界)。

---

## CHANGELOG-040 · 五條小的,其中兩條會讓整條管線停

| | |
|---|---|
| 發現日 | 2026-09-21(稽核 M007/M019/M026/M041/L001/L008 複查) | 揭露日 | 2026-09-21 |

### M041 🔴 `_series` 在「沒有 Epoch 1 那一段」時直接拋例外
`base` 只在 Epoch 1 那一支被賦值,而作者寫的退路
`base if full else START_CAPITAL` 是**死碼** —— `full` 開頭就有一個元素,`if full` 恆真。
所以 history 全部落在 Epoch-2 之後時(日後開 Run #2、或狀態檔只還原後半段)
→ `UnboundLocalError`,**整條管線停**。實跑重現過。
⭐ **一個永遠走不到的 fallback,跟沒有 fallback 一樣** —— 而且它讓人以為已經處理過了。

### M007 🔴 內部頁寫死「5 個腦」,而它自己畫 9 條線
文案點名「規則腦+GPT+Gemini+Claude+持久腦」,**漏掉純淨腦與持久腦之一**;
標題寫「五腦權益走勢」。而行為端 `m.brains.length` = **9**。
⭐ 照那句話去對圖例,會把第 5 條線(純淨腦,**消融診斷組**)誤認成持久腦
(**存活優先**)——**兩者角色相反,結論會整個反過來。**
→ 文案一律由 `m.brains` + `LABEL` 組出來。

📌 **我第一版又只修了一邊**:改完內部頁就提交測試,而公開頁的「五種完全不同」
與靜態 `252` 沒動 —— **是 parametrize 過兩頁的守衛當場抓到的**。
⭐ 那條守衛是 2026-09-04 為了同一種錯加的,今天證明它真的在工作。

### M026 / L001 🔴 讀不到門檻時,靜靜留著寫死的 252
`if(_gd && m.gate_days) ...` —— 讀不到就什麼都不做,
而 HTML 裡寫死的 `252` 原封不動留在畫面上。
⭐ 「查不到」被畫成「這就是門檻」,而這一頁的賣點正是「門檻預先錨定」。
→ 靜態 HTML 不再帶數字;讀不到明講「(載入失敗)」,與 §4 門檻的處理方式一致。

### L008 今日首次違規的分母也只含「曾違規的腦」
Gemini 根本不在那個 dict 裡,前端 `fv[b]||0` 把它畫成跟真正的 0 一樣。
這次方向剛好無害(真值就是 0),但**判準不能靠運氣**。

### 順帶:內部頁的擁擠宣稱落後公開頁 15 天
公開頁 2026-09-06 已更正「擠在一起時它反而降曝險」(實測相關 **+0.36**,方向相反),
**而內部頁一直掛著原句** —— ⭐ 同一個錯只修一邊,**第四次**。
新增守衛:那句話只能以**引述舊說法**的形式出現,不准當成主張
(判準看**每一處的上下文**,不是整頁有沒有那個字串 ——
「引述 vs 主張」今天是第五次)。

**驗收**:14 條新測試;**8 個突變全部轉紅**。全套 3,801 通過。

---

## CHANGELOG-041 · 「這一格算不出來」被印成「這一天不賺不賠」

| | |
|---|---|
| 發現日 | 2026-09-21(稽核 M027/M040 複查) | 揭露日 | 2026-09-21 |

`shadow/daily_returns.py` 的檔頭自己寫著它是**每日報酬的唯一權威來源**,
而它裡面有兩個同型的洞,兩個都是同一句話:**「查不到」≠「沒有」≠「0」**。

### M027 🔴 拿不到 equity 就填 `0.0`,公開表格印成綠色的 `+0%`
```
if eq is None:
    out.append(0.0)      # ←「沒有資料」被寫成「這天不賺不賠」
    continue             # ← prev 不更新,下一格變成跨期報酬
```
一格缺漏會變成畫面上一個**綠色的 +0%**;而且 `prev` 不更新,
下一個有值的日子會拿更早的基準去除 —— 48 小時的報酬混進日報酬序列,
波動度與由它推出的全部風險指標一起失真。
⭐ 同一個洞 2026-09-06 已經在 `_stats` 修過一次(CHANGELOG-020),
  而**這支唯一權威來源原封不動** —— 一個 codebase 兩個口徑。

→ 三種算不出來一律回 `None`:①這一格缺 equity ②**前一格**缺(分母不是前一日)
③前值為 0(從 0 起算的報酬未定義)。新增 `return_coverage()` 給分母。

**四個消費者全部跟著改**(單一權威的意義就在這裡):
儀表板送 `null`、TG 戰報印「—(算不出來)」、日記分出「還沒結算 / 當日淨值缺漏」、
**餵給各腦的近期績效送 `None` 而不是 0** ——
🔴 最後這條比顯示嚴重得多:餵錯的績效會**直接污染實驗本身**,
不只是報表難看(2026-07-25 那次就是這樣)。

### 🔴 前端那半:`null >= 0` 在 JavaScript 是 **true**
後端改送 `null` 之後,公開頁那行 `v>=0?'綠':'紅'` 會把缺漏格塗成**綠色的 `+null%`**。
⭐ **修好了後端卻讓前端把 null 讀成 0,等於沒修** —— 同一個洞換一個地方再開一次。
→ 一律先擋 `null` 印「—」並附上為什麼算不出來。
權益圖同理:`Math.min(...)` 會把 `null` 當 **0**,一格缺漏就讓整張圖的比例全錯;
現在刻度先濾掉 `null`,線遇到缺口**斷開**並在圖上寫明
「我們不補平、也不拿前一天頂替」。

### M040 🔴🔴 一個把「沒問到」當成「沒問題」的對帳函式
```
stored = (h.get("returns") or {}).get(b)
if stored is None:
    continue            # ← 帳本沒有 returns 可比 = 通過
```
而這個函式的 docstring 自己寫著
「**這個函式存在的目的就是讓這種錯不會再無聲發生**」。
對帳的分母只有「剛好帶了 returns 欄位的那些格子」,而且**從不回報涵蓋率** ——
哪天整批 returns 掉了,頁面會顯示一個漂亮的「0 筆不一致」。

→ 分成 `mismatch`(真的不一致)/ `missing_returns`(帳本沒得比)/
`underivable`(帳本有值但我們算不出來)三種,**沒比到的不算通過**。
公開頁現在印「對帳涵蓋率:**已比對 X/Y 格**」,沒比到的另外點名。

📌 **今日實測 549/549 格全部算得出來**,所以這兩條是**潛伏**缺陷,
不是正在發生的錯報 —— 而這句話由程式每天重算,不是我們記得。
「不一致 22 筆、修正日之後 0 筆」的數字**沒有任何改變**。

### 順帶抓到的兩個(都是修上面那兩條時撞出來的)
1. `emit_dashboard.build()` 與 `forward_multi._series()` 對缺一格 equity 直接 `KeyError`。
2. 🔴🔴 更嚴重:每日跑批把「公開頁發佈 / 外部錨定 / 帳本備份」三段各自包在
   `except` 裡只記一行 `[warn]`,然後照樣印「=== 完成 ===」並以 **exit 0** 結束。
   也就是說 —— **公開頁可以整份停更,而排程看到的是一個成功的跑批**。
   當時唯一的攔截是讀者瀏覽器裡那個 40 小時的新鮮度橫幅;
   ⭐ 一個要等 40 小時、而且只有讀者看得到的東西,不是告警。
   → 三段失敗都進失敗清單,結語點名,離開碼非 0。

### 兩條舊測試的斷言被改掉(要講清楚)
`test_zero_equity_does_not_produce_infinity` 原本斷言歸零隔天 `== 0.0`,
`test_missing_equity_is_zero_not_crash` 原本斷言缺格 `== 0.0`。
⭐ **兩條都是把缺陷釘死的測試** —— 守衛守的是行為,
而當初寫下的那個行為本身就是錯的。原意(不得 inf/NaN、不得 crash)完整保留。

**驗收**:20 條新測試;**19 個突變全部轉紅**
(其中 3 個第一版假綠:算出來卻沒串接進畫面、守衛掃到我自己的註解、
把「完成」加在判斷之前就繞過去 —— 判準都已加強)。
全套 **3,820 通過 / 2 skipped**。
瀏覽器端到端實測:挖掉 2026-09-21 規則腦那一格 → 表格印「—」、
曲線斷一次(完整資料時不斷)、更正聲明印「已比對 548/549 格…1 格沒有比到…不算通過」。

---

## CHANGELOG-042 · 一盞設計上不可能亮的燈

| | |
|---|---|
| 發現日 | 2026-09-21(稽核 M019/M042 複查) | 揭露日 | 2026-09-21 |

### M042 🔴🔴 「當日未能結算 0 / 61 個決策日」—— 那個 0 是**量不到**,不是沒發生
這一頁的事件表有一列「當日未能結算」,61 天全是 0。
而 `unsettled` 這個欄位**全程沒有任何寫入端** ——
程式裡沒有一行會把某一天標成「未能結算」,所以它結構上永遠是 0。

兩份模組契約卻寫著:
> 任一持倉幣無法估值 → 該腦當日標記 UNSETTLED,**不計入正式績效**

**這道防線不存在。** 缺價時真正走的是另一條路:
以**最後已知價**估值並標記 `stale`,連續無報價才折價強制出場 ——
那一天的估算值**照樣進入公開的 equity_curve 與 metrics**。
而每日日誌還天天印「UNSETTLED 0」,讓人以為防線在運作、只是沒觸發。

⭐⭐ **一盞設計上不可能亮的燈,比沒有那盞燈更糟。**
沒有那一列,讀者知道自己不知道;有那一列,讀者以為自己檢查過了。

→ 兩份契約改成描述**現況**(原句標示為已更正);
事件表那一列改印「**機制未啟用**」,並寫明
「這一格不是『沒發生過』,是**量不到**」。

**🔴 要不要真的把那道防線做出來,會改動已經公開的績效數字 —— 留給 CEO 拍板。**
在那之前,頁面說的是實話:它沒有啟用。

守衛**兩個方向都驗**:現在沒有寫入端 → 旗標必須是 False;
哪天真的有人實作了,測試會**轉紅**,逼旗標改成 True、那一列自動變回正常顯示。
(沒有後面這半,這份守衛自己就是下一個「永遠綠」的關卡。)

### M019 卡片的樣本數用了**全體**天數,而報酬率的分母是**逐腦**天數
卡片上的報酬率來自 `metrics[b]`(分母 = 該腦自己的天數),
底下那行小字卻印 `meta.epoch2_days`(全體共用)。
未結算日與缺格淨值都不計入該腦的 metrics —— 一旦分岔,
讀者會拿一個比真實樣本**大**的分母去理解那個報酬率。

→ 改讀逐腦天數;分岔時卡片自己標「⚠️ 此腦有 N 日未計入,上方報酬率的分母是 N 日」;
拿不到就明講「天數未提供」,不退回全體值再印一個假分母。

📌 今日實測 9/9 都是 49 日,**沒有分岔** —— 這是潛伏的錯,不是正在發生的;
而這句話由程式每天重算,不是我們記得。

### 順帶:一條守衛掃到了自己要防的東西
`test_事件揭露要帶分母_而且逐日算` 會掃事件區塊裡有沒有**寫死的日期**,
而它掃的是整段原始碼 —— 於是一則說明**註解**裡的日期就把它弄紅了。
⭐ 它自己上面三則註解都在講「掃字串量到的是鍵名,不是行為」,
而它本身犯了同一個病。改成先剝註解再掃,並以突變確認它仍抓得到真正寫死的日期。

**驗收**:6 條新測試;**9 個突變全部轉紅**(含反向的一條:
真的加上 `unsettled` 寫入端而旗標沒跟上 → 轉紅)。
全套 **3,827 通過 / 2 skipped**。
瀏覽器實測:注入「Gemini 少 5 天」→ 9 張卡只有 Gemini 出現警語,其餘仍印 49 日。

---

## CHANGELOG-043 · 宣稱與量測之間沒有連線

| | |
|---|---|
| 發現日 | 2026-09-21(稽核 L002/M004/M030/M033 複查) | 揭露日 | 2026-09-21 |

四條看起來不相干的稽核,其實是同一件事的四個面:
**這一頁上的宣稱,和真正在量它的程式,之間沒有連線。**

### M033 🔴 「曾達 174.6%」是一次人工抽樣,沒有任何程式在維護它
這一頁寫著「實測規則腦的實際總曝險**曾達 174.6%**」。
那是 2026-07-25 的**一次人工抽樣** —— 沒有任何程式在持續量它,
而且**連事後都重建不出來**:帳本只存目標權重(實測 61 天最高 100.0%),
存不下市值漂移後的實際曝險。

⭐ 規則腦全期回撤 60%。**固定名目部位配上腰斬的淨值,曝險再創新高完全合理** ——
而頁面會永遠停在 174.6%,讀者把一個單日抽樣值當成整段實驗的上界。

→ 從 2026-09-21 起**逐日記錄**;那三處文案改成「一次人工抽樣,**不是上界**」;
新增逐日量測欄,一律帶**量測起始日與已量測日數**。
還沒累積到資料時明講「這一頁對『實際曝險最高到哪』**沒有答案**」。
🔴 **歷史不補** —— 補不出來的東西不准假裝補得出來。

### M004 🔴 鏈裡的證據躺了兩個月,沒有人讀
錨定鏈忠實記錄了每一次程式碼變動,而 PROTOCOL §9 寫著修補
「需記錄於 CHANGELOG 並標註生效日」—— **這條規則從來沒有被任何程式檢查過**。

實測:**13 次**動到會影響公開績效的模組。
其中 **5 次**當天同步更新了 CHANGELOG;**4 次**當天沒同步、但 CHANGELOG 裡有提到那個日期
(**「提到」不等於那次變動被說明過**);
還有 **4 次(錨點 14、17、31、35)在 CHANGELOG 裡完全找不到對應**。

那幾天,算公開績效的程式改過,而我們沒有說。
⭐ 讀者若真的照這一頁邀請的方式去核對,會撞到這件事 ——
**證據早就躺在鏈裡,只是沒有人讀。**

→ 每天算、發佈、印在「自己驗」那張卡上,**連同我們自己的缺口**。
守衛:新增的缺口一律紅;而且那張豁免名單**本身也被守住** ——
放寬或清空它都會紅。(一個可以隨手放寬的基準線不是基準線。)

### M030 卡片的上半和下半不是同一天的持倉
每日順序是 ①結算(用**昨天**的部位)→ ②決策 → ③換倉,而報酬在換倉扣費之後才算。
所以卡片上半的權益與報酬,是**前一決策日**那組持倉的結果;
下半的幣種與理由,是**今天剛開**、明天才會有損益的新倉。
兩者上下並排,**零時間標示**。

⭐ 讀者(包括我們自己)必然把 +X% 歸因給看到的那組幣,再據此判斷「這個腦選幣準」。
**兩個數字各自都是對的,歸因錯了。**

→ 報酬旁邊標「截至 X 結算,由 **Y** 決定的那組持倉產生」;
持倉區上方標「下方是 X 的**新決策**,與上方報酬**不是同一組持倉**」。
今天沒取得決策的腦不印這句(它們下方本來就是舊倉,既有橫幅已說明)。

### L002 真錢門檻有三個並存的說法
已錨定的 PROTOCOL §5 只說「**≥252 個決策日**,且必須涵蓋:一次 ≥20% 回撤、
一次 ≥30% 單邊上漲、高波動與低波動各一段」。
而程式裡有一個 `GATE_DAYS_MAX = 500`,於是內部戰報說「需 **252-500** 日」、
這一頁說「門檻是 **252** 日」——
**同一個問題在兩個管道得到兩個答案,而看這兩個管道的是同一個人。**
⭐ 「實際上可能要跑比 252 更久」的正確講法是**那四個情境條件**,
不是再發明一個看起來很精確、而讀者無從查證也無從推翻的數字。

→ 500 刪除,兩邊都回到協議原話。
守衛:文案講出來的門檻數字必須能在 PROTOCOL.md 裡找到。

**驗收**:10 條新測試;**20 個突變全部轉紅**。
⭐ 第一輪有 **5 條是假綠**,全是同一型 ——
**釘了「字串在檔案裡」,沒釘「它真的會被執行、被讀到」**:
函式定義了卻沒被呼叫、容器被拿掉整段直接 return、顯示條件被改成恆假、
測試只走得到「還沒開始量」那條早退路徑、豁免名單可以隨手放寬。
全套 **3,837 通過 / 2 skipped**。

---

## CHANGELOG-044 · 剛加的那道檢查,自己就永遠少算一次

| | |
|---|---|
| 發現日 | 2026-09-21(CHANGELOG-043 部署後自查) | 揭露日 | 2026-09-21 |

CHANGELOG-043 加的「程式碼改過幾次 vs 揭露了幾次」,發佈出去是 **13 次**,
而同一時刻鏈上已經是 **14 次**。

根因是順序:`emit_dashboard` 跑在 `anchor()` **之前**,算到的鏈少了今天那一筆。
⭐ **永遠差一,不會當機、不會告警,只會安靜地少一次** ——
而少掉的那一次,正好就是「**今天剛改的程式**」,最需要被看見的那一次。

📌 這跟 2026-09-05 抓到的「`meta.anchor` 永遠落後一筆」是**同一個病**,
而那次的修法(`sync_dashboard_anchor`)就在旁邊 —— 我加新欄位時沒有想到要走它。

→ 走同一條路:append 之後補寫回去,而且補在**取公開檔雜湊之前**,
所以它仍在鏈的保護範圍內。

🔴 第一版補寫時,傳進去的那一筆**沒有帶程式碼雜湊**,
於是比對把「欄位不在」讀成「七個檔案全改了」—— 又一次「查不到 ≠ 沒有」。
已改成帶真正的雜湊(實測:今天那一筆正確地只列出 `shadow/forward_multi.py`)。

### 順帶:那個「只准改錨定欄位」的守衛
它原本寫死成「除了 `meta.anchor` 以外一個字都不准改」——
而 `code_change_audit` 同樣是**純粹由鏈推導**的狀態,也必須在這一步寫。
→ 改成**明確的白名單集合**(兩項),再多加一個欄位就會紅;
不會像從前那樣「順手多改一個」而沒有人發現。
(突變實測:在那一步偷改 `metrics` → 轉紅。)

**驗收**:3 條新測試;**3 個突變全部轉紅**。全套 **3,839 通過 / 2 skipped**。

---

## CHANGELOG-045 · 修好了一半,剩下那一半留在靜態文案裡

| | |
|---|---|
| 發現日 | 2026-09-21(稽核 M006/M012/M020/M043/M045 複查) | 揭露日 | 2026-09-21 |

這五條的共同形狀:**主症狀修好之後,剩下那一半留在別的地方** ——
靜態 HTML 文案、或另一個檔案裡。

### M043 🔴 「總曝險上限 100%(不使用槓桿)」是一句無條件的安全宣稱
它印在第一張卡片上。而規則腦與 3 個市場基準的權重由程式直接產生,
**根本不經過那道檢查**;而且部位是按名目金額開的,
淨值下跌時實際曝險會自己升上去 —— 規則腦全期回撤 60%。

但書確實存在,在**29 行外的另一張卡**。
⭐ **揭露要放在宣稱旁邊,不是另一個區塊。** 這一課這個專案上了四次。

→ 適用範圍寫進同一段,連同 2026-07-25 人工抽樣的 174.6%
(並註明那是單日一次量測、不是上界)與逐日量測的起始日。

### M045 「5 個 AI 腦」其實只有 3 個模型家族,而卡片從來沒說
持久腦 = **Claude**(換 prompt)、純淨腦 = **GPT**(拿掉規則訊號)。
同一頁的擁擠度卡早就印著「跨 **3** 個模型家族」——
兩個數字講的是同一件事,而只有一邊說了。
→ 卡片副標補上底座模型。

### M006 / M020 同一個數字一頁三份,兩份是手寫的
副標的「9 位競爭者」早就改成從資料算,而頂部警語與計畫卡**仍寫死** ——
加一位競爭者,同一頁會出現三個答案。
→ 兩處都改成由資料填;讀不到就明講,不留一個看起來正常的數字。

### M012 🔴 守衛在「檔案不在」時 **skip**,而 skip 是綠燈
`test_lab_page_no_truncation.py` 有 **8 處** 這樣的 skip,
而那份檔案上面自己寫著「**找不到檔案要紅,不准 skip**」。
它們檢查的是已經跑了 61 天的**已發佈**檔案 —— 不在就是故障本身。
→ 8 處全改成 fail 並印出掃描路徑;另外在兩支錨定測試裡找到 3 處同型的,一併改。
新增跨檔守衛:任何「判本地檔案在不在就 skip」且涉及已發佈檔案的,一律紅。

📌 **這條守衛第一版自己誤報了**:它用「附近有沒有出現檔名」判斷,
把一條**連不上外網才 skip** 的測試判成紅(那條的網址裡剛好有 `dashboard_data.json`)。
⭐⭐ 「連不上公開站」≠「檔案不在」;而**會誤報的守衛遲早被關掉,比沒有更糟**。
判準已改成看 skip 的**條件**本身。

**驗收**:6 條新測試;**6 個突變全部轉紅**。全套 **3,851 通過 / 2 skipped**。

---

## CHANGELOG-046 · 守衛自己不會響,而頁面整個壞掉時測試全綠

| | |
|---|---|
| 發現日 | 2026-09-21(對 CHANGELOG-041~045 的自我複查) | 揭露日 | 2026-09-21 |

今天修完五條之後,我對自己的修法做了一輪**對抗性複查**。
抓到的東西比原本要修的還嚴重,而且**有三個是我今天自己做出來的**。

### 🔴🔴 最嚴重的:新鮮度守衛「拿不到資料就安靜放行」
```
if(!s||!snapUTC) return;     // 後端沒給快照時刻 → 整段檢查直接跳過
```
而副標照樣印一個時間(用**帳本的決策日**冒充快照時刻)。
結果:**資料凍再久,畫面和正常時一模一樣** —— 正是這個守衛存在的目的。
而這條岔路走得到:後端讀鏈是 `except Exception: pass`,鏈一壞就靜默出貨。

→ 改成 fail-**loud**:拿不到快照時刻、或時刻格式解析不出來,都插一張紅牌
「這一頁無法判斷資料有多新,請勿當成即時」。副標的 fallback 不再拿決策日頂替。

### 🔴🔴 門檻比它要防的那場事故還寬
紅燈門檻是 **40 小時**,而唯一一次真實事故(2026-09-02~04)是 **33.5 小時** ——
⭐ **一盞對唯一一次真實事故不會亮的燈,設計上就不可能紅。**

→ 門檻改用**實測**,不是想一個數字:鏈上 62 個相鄰發佈間隔,
**61 個 ≤ 24.0 小時**,唯一例外 48.0 小時(2026-07-28 漏發)。
分兩級:**30 小時**「比正常晚」(黃)、**40 小時**「可能停擺」(紅)。
30h 有零歷史誤報,而 33.5h 那場現在會亮。
(實跑五種情況:20h 不亮、33.5h 黃、48h 紅、沒有快照紅、格式壞掉紅。)

### 🔴🔴 推不出去仍然回報成功
`anchor()` 的 `ok` 是寫死的 `True`,六處只改 `pushed`;
而呼叫端只在 `except` 裡記失敗 —— **真實事故(git push 被拒)是回傳值,不是例外**。
⭐ 我今天早上才加的那道「發佈失敗要大聲講」,對它要防的那場事故**完全無效**。
→ `ok` 改成從結果算出來、單獨執行時推失敗要非 0 結束、
跑批改看回傳值(抽成純函式 `publish_failure_reason` 才測得到)。

### 🔴 我昨天寫死的一句話,今天就是假的
「適用範圍」那段散文裡有一句「**逐日量測已於 2026-09-21 開始,結果印在下方表下**」,
而同一份資料檔 `measured_days = 0`、下方由資料生成的那行印的是「**尚未累積到資料**」。
**同一頁兩句話互相打臉,而其中一句是我手寫的。**
→ 整段改由資料生成(受檢查名單讀 `violation_checked`、逐日量測共用同一個函式);
人工抽樣的 174.6% / 2026-07-25 收斂成**單一常數**供三處共用。

### 🔴 守衛的名字承諾兩頁,而其中一頁的分母是空的
今天新加的「兩頁都不准再承諾樣本夠了就顯示 Sharpe」,
在**公開頁上觸發 0 次** —— 它只在上下文含某個措辭時才斷言,而公開頁用的是別的說法。
→ 加上**空分母自檢**:觸發次數 0 要紅,不准當成通過。

### 🔴 內部頁一次坐實五條:公開頁修好的它一個都沒跟上
不顯示最大回撤(而規則腦 60.8%、等權多頭 59.7% 早已越線)、不顯示帳本對帳、
`fetch` 沒有 `.catch`、仍掛著已更正的 Sharpe 條件式承諾、沒有任何新鮮度揭露。
⭐ 「同一個錯只修一邊」在這個專案是**第五次**。
→ 五項全部補齊,並把守衛 parametrize 過兩頁,以後只修一邊過不了。
補的過程順手在**公開頁**也發現同型的洞:§4 表少了某位競爭者的資料時,
那一列會印一排「—」而判定欄印**綠色的「未觸發」**。已改成「缺資料不等於未觸發」。

### 🔴🔴 最後:測試全綠,而頁面整個壞掉
改完之後全套 **3,873 條全綠**,一開瀏覽器整頁停在「載入中…」——
主控台一行 `SyntaxError`。兩個錯都在巢狀樣板字串。

⭐⭐ 這一頁的守衛全都在**掃字串**。字串都在,所以全綠;
而那些字串**一個字都沒有被執行過** —— 整份 JS 根本沒有 parse 成功。
→ 新增守衛:抽出每一段 `<script>` 丟給 `node --check`,三個公開頁都驗。
沒有 node 的機器要**紅**,不准 skip。(突變實測:把那兩個語法錯放回去,兩次都轉紅。)

**驗收**:新增與加強 20 條守衛;**26 個突變全部轉紅**。
全套 **3,876 通過 / 2 skipped**。瀏覽器端到端實測五種新鮮度情境。

---

## CHANGELOG-047 · 這一頁印著一份從未成交的購物清單

| | |
|---|---|
| 發現日 | 2026-09-21(稽核 A019 / L009 複查) | 揭露日 | 2026-09-21 |

### 🔴🔴 今天就在發生

2026-09-21 是**基建保護日**:決策前後的報價偏離超過保護門檻,
依規則當日**強制只減倉** —— 只有會降低總曝險的調整才執行,其餘一律維持原倉。

而這一頁的「各腦現況」卡片,印的是規則腦持有 **ARB 12.5%**。

帳本裡 **沒有 ARB**。實際持倉是 **ZEC 13.1%**。

⭐ 卡片上的幣種與權重,從第一天起就 100% 來自各腦**要求的目標**(`current_alloc`),
而後端**從來沒有發佈過**由帳本 `units` 導出的實際部位。
正常日子換倉會成交,所以兩者接近、看不出來;
**基建保護日整天不換倉,那份清單就是一份從未成交的購物清單** ——
印在一個寫著「✓ 公開每個決策與理由」的頁面上。

61 個決策日裡已經發生 **3 次**(08-10 / 09-01 / 09-21)。

### 讀者會被誤導到什麼程度

1. **歸因錯**:今天規則腦的 ARB 是虛構持倉。任何人拿它去看「AI 選幣準不準」,
   評的是一筆**不存在的交易**。
2. **風險姿態失真**:「曝險 100%」算的是**目標**,而實際市值漂移後的曝險
   既沒量到也沒印(正是 174.6% 那件事想講卻還沒講到的東西)。
3. 而卡片還**主動宣稱**「下方是 2026-09-21 的新決策」——
   那句話在基建保護日對規則腦與市場基準是**假的**。

### 修法(只新增欄位,不動任何已公開的績效數字)

後端新增三樣,`equity` / `returns` / `metrics` 一個字都沒碰:
- `current_position` —— 由帳本 `units` × 最後已知價換算的**實際**持倉權重。
  🔴 有持倉卻取不到價時回 `None`,**不准退回用目標頂替**
  (估出來的數字看起來跟真的一模一樣,那正是最危險的地方)。
- `position_priced_at` —— 沒有計價日的權重不算數。
- `events.today_infra_skip` —— 給卡片用,不寫死日期。

兩頁的卡片改成**兩排並列**:

```
🎯 要求的目標配置   AKE 13% · BR 13% · ONE 13% … ARB 13%
📦 實際持倉(帳本 units 換算,計價日 2026-09-21)
                   ONE 14% · BR 13% · ZEC 13% …
🔴 目標與實際不一致:目標有而帳上沒有 ARB;帳上有而目標沒有 ZEC
```

差集是**算出來的**,不是寫死的。基建保護日另加警示框說明「那份目標今天沒有執行」,
同時把「下方是今天的新決策」那句關掉 —— **兩句話會互相打架**。

### 順帶:基建異常日的報酬也標出來了

上半的「截至 2026-09-21 結算」後面加註
「(該日為基建異常日,換倉受我方基建限制,**不是正常的 AI 成績**)」。
原本這一天被當成正常成績看,而表格那邊早就標了 —— 又是只修一邊。

**驗收**:10 條新守衛(全部 parametrize 過兩頁);**10 個突變轉紅**
(其中一個第一版假綠:判準釘了「實際持倉」這四個字在頁面上,
而把標籤改回印目標時、`null` 分支裡的同一個詞還在 → 已改成釘「那排標籤真的從
`current_position` 組出來」)。全套 **3,884 通過 / 2 skipped**。
瀏覽器實測:9 張卡、零主控台錯誤、ARB/ZEC 差集正確印出。

---

## CHANGELOG-048 · 我們說「外部時間戳」,而那個時間戳是我們自己機器寫的

| | |
|---|---|
| 發現日 | 2026-09-21(稽核 A001/A028 複查) | 揭露日 | 2026-09-22 |

### 🔴🔴 我們對你們講過一句錯的話

這一頁與已發佈的驗證器,兩處都寫著:

> 防重算靠的是每日推到公開 git repo 留下的**外部**時間戳 ——
> commit 時間由 **GitHub 記錄,不是我們說了算**。

**那是錯的。**

git 的 author 與 committer 時間**都是提交那台機器寫的**,GitHub 不背書。
而 GitHub 的 commits 頁**預設顯示 author 時間**,author 時間在 rebase 之後**原樣保留**。

⭐ 硬證據(你可以自己驗):第 **43** 號錨點在鏈上與 GitHub 上都標 **2026-09-03**,
而引入它的 commit `03e2d9b` 的 committer 時間是 **2026-09-04 18:08:55 +0800**
—— **差 33.5 小時**,兩天份的證據同一晚落地。

```
git log --format=%cd 03e2d9b
```

那一天正是 2026-09-02~04 發佈故障那次(CHANGELOG-010)。
**鏈上與頁面對這 33.5 小時零揭露**,而我們還在旁邊寫著「由 GitHub 記錄」。

### 我們能給的與不能給的

**不能**:證明某一筆是當天被外部平台收到的。今天做不到,就講今天做不到。

**能**:`push_receipts.json`(自 2026-09-22 起側錄)——
每一筆記「我們這邊在什麼時刻、推了哪一個 commit、成功與否」。

🔴 它**仍然是我們自己寫的**。但兩件事讓它有用:
1. `chain_utc` 與 `push_returned_utc` 的**差距**,會把「當天推的」與「事後補推的」分開;
2. 它被收進錨點的保護清單 → **會被下一筆的雜湊蓋住**,事後改它會讓下一筆對不上。

⚠️ 代價是**晚一天才防得住**(entry hash 必須在 push 之前封死,
收據不可能塞回自己那一筆)。這個限制寫在頁面上,不讓你以為它當筆自我背書。

### 順帶:驗證器驗不出「少了一天」

`seq` 與 `days_recorded` **都是計數器** —— 漏跑一天它們只是少加一次,不會出現缺口。
鏈裡唯一的日期欄位 `utc` **從來沒有被拿來比對相鄰日差**。
所以真鏈裡 **2026-07-28 整天沒有錨點**,而驗證器照樣印「✓ seq 連續」並以 0 退出。

⭐ 「少了一天」跟「改了一筆」一樣是造假的形狀,而這支只防得了後者。

→ 加上缺日偵測(只用標準庫)。實跑真鏈,**抓到 2026-07-28**。

順帶:`prev` 欄位缺失時驗證器原本會**直接 crash** ——
一個遇到壞資料就掛掉的驗證器,等於把「鏈壞了」變成「驗證器壞了」,而你分不出來。

### 同一批修掉的其他十條

- **驗證指令漏了 `anchor.json`** —— 照著頁面逐字操作的人,跑完一定會看到
  「有 1 個檔案沒有比對到」。那不是被竄改,**是我們自己的清單不完整**。
- **一個 2026-09-06 就已永久撤回的承諾,在頁面上掛了 15 天** ——
  仍寫著「在重述數列完成獨立驗證**之前**…」,把「不會做」講成「還沒做」。
- **紀律紀錄表從來沒讀過 `violation_checked`**,靠的是
  「受試者名單剛好等於受檢查集合」這個**沒人寫下來也沒人測的巧合**。
- **兩個年化因子並存而一個字都沒標**:Sharpe 用 √365、IR 用 √252,
  兩個數字並排躺在同一份你會下載的檔案裡。**刻意不同,不是漂移**
  (IR 依 §3/§6 鎖死,§9 禁改)—— 現在兩個因子都發佈出來。
- **資料品質旗標一個都沒發佈**:報價中斷、資金費不完整、stale、每日不變式對帳
  (11 項檢查)全部只進日誌。今天發生數都是 0 ——
  **而沒有分母的 0 跟「我們沒有檢查」長得一模一樣**,現在每一項都帶分母。
- **61 天 × 9 位的決策理由住在一個沒有任何地方連得到的檔案裡**,
  而這一頁寫著「✓ 公開每個決策與理由」。已補上連結,並承認它先前沒有入口。
- 另有 6 處把「資金費被乘 3 倍」引成 CHANGELOG-002(那是 001)。

**驗收**:18 個突變全部轉紅;全套 **3,904 通過 / 2 skipped**。

📌 2026-09-05 那份 102 條公開面稽核,到今天**全部結案**:
94 條已修、6 條屬其他子系統、2 條已附證據交接給山寨雷達頁。

---

## CHANGELOG-049 · 「94 條已修」這個數字本身在誇大

| | |
|---|---|
| 發現日 | 2026-09-22(外部審查) | 揭露日 | 2026-09-22 |

昨天(CHANGELOG-048)我們寫著:**102 條公開面稽核全部結案,94 條已修。**

把這件事送給外部審查之後,得到一句我認為成立的指控:

> **你透過降低承諾標準來達成 100% 結案率。這叫免責(Disclosure),不叫解決(Resolution)。**

### 逐條分類之後的實話

| 修法性質 | 條數 |
|---|---|
| **真的解決**(錯的數字不再被產生) | **39** |
| 兩者都有 | 28 |
| **只是揭露**(數字一個字沒變,只是把真相講出來) | **19** |
| 只加了守衛(產品面沒動) | 4 |
| 判不出來 | 7 |

而且 —— **37 條**修完之後,**你看到的數字本身仍然是錯的或不完整的**。
我們做的是**讓你知道**,不是**讓它變對**。

例子(全部是「只是揭露」那一類):
- 「當日未能結算」那一列改印「機制未啟用」—— 那道防線**還是沒有做**,缺價日照樣進績效。
- 「曾達 174.6%」改成「單日人工抽樣、不是上界」—— 那一格**仍然沒有任何量測**。
- 持倉只印前 7 檔,改成「只顯示前 7 檔,共 N 檔」—— **還是只畫 7 檔**。
- 兩個年化因子(√365 / √252)改成「兩個都標出來」—— **沒有統一**(協議 §9 禁改)。
- 「不顯示 Sharpe」改口成「我們刻意不放」—— 頁面上**一個 Sharpe 都沒有多**。

📌 逐條的分類與理由在
[`docs/稽核/20260905_公開面稽核_102條.json`](./data/shadow/) 的 `resolution_kind` 欄
(那份檔案在我們的 repo 裡,不在公開目錄;它的摘要寫在這裡)。

### 🔴 為什麼要特地講這件事

因為「102 條全部結案」這句話,如果不附上面那張表,**就是一個會被誤讀的數字** ——
而這一頁存在的理由就是不要產生會被誤讀的數字。

---

## 同一次審查的第二個結論:24 小時的空窗

> 「收據是我們自己寫的、晚一天才被雜湊蓋住,
>  意味著在每日結算的當下,你有整整 **24 小時的特權視窗**可以重寫當日歷史而不被發現。
>  對於一個**日頻**實驗,24 小時的作惡空間等於 **100% 的作惡空間**。
>  如果不引入外部時間戳,就必須把『帳本可驗證』改成
>  『**歷史帳本(T-1)可驗證,當日帳本不可信**』。**不這麼改,就是在欺騙外行人。**」

這一句我也認為成立。做了兩件事:

**1. 先把宣稱降級。** 首頁那一行從「✓ 帳本可驗證」改成
「✓ **歷史**帳本可驗證 · **當日那一筆還不可驗**」。

**2. 然後把它修掉。** 自 **2026-09-22 / 錨點 seq 73** 起,
每一筆錨點的雜湊在**推送之前**送進 [OpenTimestamps](https://opentimestamps.org),
最終落在**比特幣區塊**上 —— 比特幣的區塊時間**不是我們能控制的**,
這是這個專案第一個**真的**外部時間。

證明檔在 `data/shadow/ots/seq<N>.ots`,而且**跟那一筆錨點在同一個 commit 裡**
(刻意設計:收據做不到這件事,因為收據是推送**結果**)。

驗證(用的是**別人的**工具,不是我們寫的):
```
pip install opentimestamps-client
ots verify seq73.ots
```

🔴 **它證明什麼、不證明什麼**:它只證明「**這個雜湊在那個時間點之前就已經存在**」。
它**不證明我們的數字是對的**,也不證明我們沒有在更早以前算過別的版本再挑一個來蓋。
剛送出的是**待確認**證明,要等區塊確認後 `ots upgrade` 才完整。

⚠️ **seq 73 以前的錨點沒有這個。** 我們**不追溯補蓋** ——
事後補蓋的時間戳證明不了當時,那會是另一句假話。
對 seq 72 以前的帳本,上面那段「當日不可驗」**仍然成立**。

### ⏳ 外部審查還有一個條件我們沒做到
> 建立**非本機、非 CI** 的外部看門狗,從外部網路直接存取公開頁與 GitHub。

這件需要 GitHub 授權與密鑰,**不在工程這一側**。在它上線之前,
「發佈停擺會被發現」這件事仍然只靠讀者瀏覽器裡那個新鮮度橫幅。

📌 另:這次外部審查只拿到**一家**的意見。另一家因為 API 額度用盡沒有回覆,
所以**這不是一次完整的雙審**,我們不把它當成通過。

---

## CHANGELOG-050 · 🔴🔴 我們把一支「無條件說通過」的假驗證器推上了這個網站

| | |
|---|---|
| 發生 | 2026-09-22 00:28:46Z ~ 00:30:44Z(**118 秒**) | 揭露日 | 2026-09-22 |

### 發生了什麼

在那 118 秒裡,任何人從這個網站下載 `data/shadow/verify_chain.py`,
拿到的是**這一支**:

```python
# [MUT-M3] 說謊版:外面的人下載到的就是這一支
def verify(chain):
    return {'ok': True, 'n': len(chain), 'checked': len(chain), 'problems': [], ...}
def main(argv):
    print('✓ 全部通過,什麼問題都沒有')
    return 0
```

**十行,不讀任何東西,無條件印「✓ 全部通過,什麼問題都沒有」。**

commit `648bd21`(那是當天的錨定 seq 73),還原於 `8416c84`。兩者都在 git 歷史裡,可自行複驗:

```
git show 648bd21:data/shadow/verify_chain.py
```

### 為什麼會發生

那是我們**自己的稽核**在做突變測試時的產物 ——
我們故意把驗證器改成說謊版,看守衛會不會抓到。
問題是**突變是就地改在公開 repo 的工作目錄裡**,
而同一時間自動錨定跑批執行了 `git add data/shadow` → commit → push。

三道發佈範圍守衛**全部放行**,因為它們**只比對路徑前綴**:
`data/shadow/verify_chain.py` 在發佈範圍內,所以合法。

⭐⭐ **範圍守衛擋得住「別人的檔案混進來」,擋不住「我們自己的檔案變成假的」。**

### 更糟的一件事:鏈本來就沒有保護這支程式

`verify_chain.py` **從來不在錨點的保護清單裡**。
也就是說:即使它被換成說謊版,鏈上也**一個字都不會記到**。

我們把「✓ 帳本可驗證 —— 你可以自己驗」印在頁面上,
卻**沒有保護那支用來驗的程式**。

### 修了什麼

1. **`verify_chain.py` 納入錨點保護清單** —— 從今天起,讀者下載的那一支,鏈會記住它的雜湊。
2. **新增第四道守衛(內容,不是路徑)**:推送之前比對公開目錄的驗證器與引擎裡那一份,
   **不一樣就中止發佈**。已實測會擋。
3. 突變測試不再就地改公開 repo(那是這次的直接原因)。

### 我們不做的事

**不淡化。** 這 118 秒裡如果有人下載了它,他拿到的是一支會對任何東西說「通過」的程式。
我們不知道有沒有人下載 —— 沒有那個紀錄,所以也不會假裝有。

這條寫在這裡,是因為這一頁的規矩是**講錯要留痕,不是把錯的東西刪掉就算**。

---

---

*本檔隨每日錨定推送至公開 repo。任何後續更正以新條目追加,不修改既有內容。*
